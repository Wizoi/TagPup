{
  "name": "tagpup-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Test-only tooling for the TagPup web interfaces. The apps themselves ship no JavaScript dependencies and are served as plain files.",
  "scripts": {
    "test": "node --test \"tests/frontend/*.test.mjs\""
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
