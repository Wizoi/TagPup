# Findings

This file records every review finding, bug report and known problem: what was found, and where it stands. A finding that exists only in a conversation gets lost, and a decision that is not written down gets made again.

- **Add a row when something is found**, before fixing it.
- **Status** is one of:
  - `open`
  - `fixed`: give the commit, by its hash or, for a row added in the same commit, its subject.
  - `not reproduced`: say what was tried.
  - `left as is`: say why.
  - `tabled`: say until when.
  - `planned`: give the phase in [ARCHITECTURE.md](../ARCHITECTURE.md).
- **Decisions the owner makes** are marked *(owner, date)*. Don't reopen them without a new fact.
- **Never put real names here.** The library is photographs of real people.

Findings fixed before 2026-09-23 are in the git log (`git log --grep="^fix"`). This file starts with what was still open or decided on that day.

| # | Found | Finding | Status |
|---|---|---|---|
| 1 | 2026-09-23 | Faces are detected on the photo as stored, so a sideways photo (about 1 in 600, more as photos are rotated) is searched sideways. A rotated TIFF also keeps the embedding of its old turn. Detecting upright changes the coordinate system of stored face boxes, the crops, and the rotate logic. | tabled for a future discussion *(owner, 2026-09-23)* |
| 2 | 2026-09-23 | One of the library's two PNGs cannot be rotated. The rotate refuses and changes nothing. | left as is *(owner, 2026-09-23)* |
| 3 | 2026-09-23 | Six faces in `photo_index` were both excluded and named. | fixed: the owner kept the names *(2026-09-23)*; exclusions lifted, with a backup taken first (`photo_index.before-keep-names-20260923_180819.db`) |
| 4 | 2026-09-23 | Rows had people missing that their keywords or faces name: 741 names on 597 rows in one library, 885 names in the other. | fixed: `refresh_rows_from_files.py --apply` on both libraries (550 and 591 rows), with a backup taken first. Phase 4 derives people instead of storing them. |
| 5 | 2026-09-23 | Opening New Person takes about 1.7 s. Caching its embeddings would hold about 388 MB in memory. | open: revisit when embeddings get their own table (phase 4) |
| 6 | 2026-09-23 | Duplicate face-resolution trace entries for paths that differ only in case. | not reproduced. Each library now writes its own trace file. |
| 7 | 2026-09-23 | `backups/` holds 28 GB in 41 copies. Scripts also wrote copies next to the code they ran from. | fixed *(owner, 2026-09-23)*: the old copies were deleted, keeping the newest of each library and the `.log`/`.txt` records. New backups go beside the library, in `data/backups/`, since *refactor: Library names a library and what belongs to it*. |
| 8 | 2026-09-23 | Eight TagTuner routes are reached only by tests: its copies of rename, time shift, delete, open in Explorer, rotate, save metadata and bulk tags, plus `/api/faces/recluster`, which no page calls. | fixed: *refactor: TagTuner loses the routes its page never called*. Its tests of them moved to TagPup's copies, which they had not been run against; the moves found #21. |
| 9 | 2026-09-23 | `runner.py` opened the database with `sqlite3.connect`, without WAL mode or the write lock. The database guard never saw it: each guard kept its own list of files, and that one skipped `runner.py` and the launchers. | fixed: *refactor: the foundation modules move into tagpup/*. Every guard now checks one list. |
| 10 | 2026-09-23 | `runner.py` keeps its own copy of the `photos` and `faces` schema, and two column migrations, separate from `PhotoIndex`'s. Two definitions of one schema drift apart. | fixed: 2d42646 (*refactor: one owner for the schema, with versioned migrations*, #48). The runner calls `tagpup.store.schema.ensure`. |
| 11 | 2026-09-23 | `tests/test_multiple_databases.py` changes the checkout's own `config.ini`: selecting a library through the API makes the server write `default_db` there. The test restores the old value afterwards, so a run stopped in between leaves the app pointing at a test library. | fixed: *refactor: one owner for config.ini, and TAGPUP_HOME*. The test runs in a home of its own and checks that the checkout's config is byte-for-byte unchanged. |
| 12 | 2026-09-23 | Moving the foundation modules into `tagpup/` (9d7e1c4) broke both measurement tools. Their sandbox copied a fixed list of folders that did not include `tagpup/`, so the sandbox server could not import its database module. Nothing tested a sandbox. | fixed: *fix: measurement sandboxes copy tagpup/ too*, with a test that imports both servers inside a sandbox that cannot see the repo |
| 13 | 2026-09-23 | The CLI and the servers name the main library's taxonomy file differently: `photo_taxonomy.json` (CLI) and `photo_index_taxonomy.json` (servers). The owner's `data/` holds both, written on different days and already different in size. The special case is in `tagpup_cli.get_db_paths` and in two places in `scripts/taxonomy.py` (`TagTaxonomy`, `seed_taxonomy_from_db`); `Library.taxonomy_file` follows the servers. | fixed: *refactor: the tag tree lives in the library only*. The CLI works on one library (`get_db_path`), and `TagTaxonomy` takes it. |
| 14 | 2026-09-23 | The test suite writes its libraries into the checkout's `data/`: about 20 databases in the owner's folder, some without the `test_` prefix (`created_db_2.db`, `unused.db`) and one with a 2.9 MB WAL left behind. A hard-coded exclusion list, copied into `runner.py` and both servers (one list since *refactor: one set of rules for the library picker*: `tagpup.core.library.NOT_LIBRARIES`), hides some of them from the library picker. | open: tests move to their own `TAGPUP_HOME`, then the exclusion list goes. The existing files were deleted *(owner, 2026-09-23)*, but each run in the checkout writes new ones until then. |
| 15 | 2026-09-23 | Creating a library leaves it open: both servers' create handlers load a `PhotoIndex` for the new file and never close it, so the connection lasts until a garbage-collection pass. On Windows the file cannot be moved or deleted until then. Found when a test could not delete its temporary home. | fixed: *fix: creating a library closes it again*. One `create_library` replaces five copies (both servers, both launchers, the runner), and a test with garbage collection off deletes the new file. |
| 16 | 2026-09-23 | Selecting a library saves its name without checking that it exists. The next start then creates an empty library under that name. | open |
| 17 | 2026-09-23 | `config.ini` is tracked in git, but the app writes the last-opened library into it. Every switch shows up as a change, and `default_db` has been committed back and forth three times (fd8a9dc, be7e06c, 349c5fe). | fixed *(owner, 2026-09-23)*: *chore: config.ini is each installation's, not git's*. `config.example.ini` is tracked, setup copies it, and the defaults match it. |
| 18 | 2026-09-23 | Moving `db.py` into `tagpup/store/` (9d7e1c4) moved its default backup folder too: it was found two folders above the module, which became `tagpup/` -- inside the code, where the measurement sandbox would have copied every backup. No backup had landed there yet; no test covered the default folder. | fixed: *refactor: Library names a library and what belongs to it*. Backups go beside the library, and inside `tagpup/` only `tagpup/config.py` may find folders from `__file__`. |
| 19 | 2026-09-23 | `scripts/merge_duplicate_person_tags.py` turns tags into leaves by hand (`leaf_of`), which CLAUDE.md forbids; the vocabulary helpers own it. | fixed: *refactor: one reading of a tag*, with 54 other hand-written splits |
| 20 | 2026-09-23 | The indexer's per-photo write locks lived in `data/locks` under the working directory. The indexer a server starts runs in the code's folder, so one started by hand from anywhere else locked in a different folder, and the two could write the same photo at once. An installed copy would have made that the normal case. | fixed: *fix: indexers lock photos beside the library, not the working directory* |
| 21 | 2026-09-23 | A request with no library in its URL, or naming the startup library, went to `data/<its name>.db`, not to the file the server was started on. Same file while the library lives in `data/`, as the launchers arrange; a server started on one anywhere else served an empty namesake it created in `data/`. Found when moved tests put their library in a temp folder. | fixed: *fix: a server serves the library it was started on, wherever it is* |
| 22 | 2026-09-23 | TagPup's indexing messages tell the owner to "run Recluster" (`index_folder_with_cli`), but no page has a Recluster button: clustering is the runner's **Run Identity Resolution Clustering** or `tagpup_cli.py cluster-faces`. | fixed: *fix: adding a folder names the clustering button that exists*. A test ties the message to the runner's button label. Neither page shows the message today: TagTuner's reports only failures, and TagPup's sees no index (#34). |
| 23 | 2026-09-23 | One photo in `photo_index` carried a test tag holding a "\|", which some programs write between levels. Python read it as a separator in a few places and as part of a name in the rest. | fixed *(owner, 2026-09-23)*: corrected, then the whole test hierarchy removed at the owner's request, from the file, the index and the taxonomy. Backups first: `photo_index.before-fix-pipe-tag-20260923_214253.db`, `photo_index.before-remove-test-tags-20260923_214444.db` |
| 24 | 2026-09-23 | Nothing stops a tag or a person's name from holding a "\|", a "\\", a control character or an empty level. Typed into either page, it is written into the photo files as it is. | fixed *(owner, 2026-09-23)*: *fix: a tag other programs would misread is refused where it is set*. Both servers and both pages, held to one list of cases (`tests/tag_rules.json`). |
| 25 | 2026-09-23 | Adding a tag under one two or more levels deep builds junk nodes. "Jane" added under `Crew/Divers` made `Crew/Divers/Jane`, and also `Crew/Divers/Crew`, `Crew/Divers/Crew/Divers` and `Crew/Divers/Crew/Divers/Jane`; the reply's id named the last. Neither library holds any. `test_taxonomy_lifecycle.py` builds exactly this tree and checked only that the right node was there. | fixed: *fix: a tag added two levels down is one node, not four*. The test now checks the whole tree. |
| 26 | 2026-09-23 | Which roots hold people is written out by hand in 19 places, as five different sets, beside `TagTaxonomy.people_roots()`, which reads the library's own. The sets without People matter: kr-track files all 59 of its people under People, so the CLI's `suggest` offers each of them to CLIP by name (the server stopped doing that in `clip_candidates`), and the suggester's and the caption writer's checks do not count them as people. | open: phase 2, as the rules move into `tagpup.core` |
| 27 | 2026-09-23 | `TagTaxonomy.find_person_path` returns the first path it meets for a name, and it meets them in the order of a set, which can change from one run to the next. The suggester names recognised faces with it. `find_by_leaf`, `people_paths_for` and `repair_bare_person_tags.py` treat a name filed twice as ambiguous and leave it alone. Two people in `photo_index` are filed twice. | open |
| 28 | 2026-09-23 | A path typed into TagPup is written as typed. "People / Rowan Thackeray" creates the node `People/Rowan Thackeray`, but the photo gets the spaced form: `resolveTagOrPerson` returns the text rather than the tag it created, and nothing on the write path normalizes a tag. Neither library holds one. | fixed: *fix: a new tag is written as the tag tree spells it*. The page returns the tree's spelling, and both servers write a new tag in it. |
| 29 | 2026-09-23 | Editing a caption renames a photo that Smart Rename named (one carrying PreservedFileName) by splitting its name on " - ": the first part is taken as the grouping and the second as the index. The owner's groupings often hold " - " themselves (`<date> - <event>`), so "`<date> - <event> - 07 - <caption>`" became "`<date> - <event> - <new caption>`", its index gone. 10,795 photos in `photo_index` have that shape, and 3 of a random 30 of them carry PreservedFileName. The name was built by a second copy of Smart Rename's code, which also misread every name made in a format other than the default. | fixed *(owner, 2026-09-23)*: *fix: a Smart Rename grouping cannot hold " - "*. Renaming moves forward, so the fix is at the grouping: a name already made that way is put right when Smart Rename next runs on its photos, and caption renaming is left as it is. It still misreads names made in a format other than the default, which the owner does not use. |
| 30 | 2026-09-24 | Renaming or deleting a tag in the tag tree, merging tags, and renaming a person rewrite each photo's whole keyword set starting from the index's copy of its tags, not the file's. A keyword the file holds and the index does not -- written by another program, or since the last index -- is dropped from the file; one the index holds and the file does not is written into it. The bulk writers were moved to the file for this reason (`tags_in_file`); this one was not. A random 200 photos in each library agree with their files today, so nothing has been lost yet. | fixed: *fix: renaming a tag everywhere starts from what each file holds* |
| 31 | 2026-09-24 | TagTuner's Tags view loads every photo of a tag at full size. Its cards ask for `/api/photo-file?path=...&thumb=1`, and no server reads `thumb`, so each card downloads the original file, where every other grid asks for a copy with `size=`. | open |
| 32 | 2026-09-24 | 13 routes, 6 in TagPup and 7 in TagTuner, decode a path or a name twice: `parse_qs` has already decoded the query, and they call `unquote` on what it returns. A name holding "%" and two hex digits reaches them as a different name: `Scan%2001.jpg` becomes `Scan 01.jpg`, and the photo is not found. Neither library holds such a path today (0 of 68,327 and 0 of 1,129). | open |
| 33 | 2026-09-24 | An error page that names a photo cannot always be sent. `send_error` puts its message in the HTTP status line, which `http.server` encodes as Latin-1, so photo-file's 404 for a path holding any character outside Latin-1 (Greek, Cyrillic, most Central European letters) raises instead, and the browser sees the connection drop. The servers build 36 error messages with an f-string. | open |
| 34 | 2026-09-24 | Only tests call TagPup's `/api/folder/index-start`: its page lost the Index button when adding folders moved to TagTuner. The page still asks its own server about indexing when it opens a folder, "so an index started elsewhere still shows its progress here". But an index started in TagTuner runs in TagTuner's process, so TagPup's server answers "Ready" throughout. | planned: phase 5, where one process serves both apps from one queue |
| 35 | 2026-09-24 | TagTuner carries copies of two TagPup methods that nothing calls. No route reaches `handle_get_tags`, and no code calls `rescan_folder_to_cache`. The second is the only writer of TagTuner's `folder_cache`, and nothing reads it. Three more class attributes, `suggest_threads`, `model_lock` and `shared_embedder`, are declared and never used. | fixed: *refactor: TagTuner loses the copies nothing called* |
| 36 | 2026-09-24 | Marking a node as holding faces, or hiding it, marks the nodes under it with `tag LIKE '<path>/%'`. LIKE ignores case and reads `_` and `%` as wildcards, so marking `Club_A` would mark everything under `ClubXA` too. No tag in either library holds `_` or `%` today. | fixed: *fix: marking a branch marks only the tags under it*. `tagpup.store.taxonomy.sql_branch` matches the path and its `/`. |
| 37 | 2026-09-24 | Opening the tag tree first repairs it. Every GET of `/api/taxonomy/tree` looks for three things: nodes missing their parent, names holding `/`, and nodes under a root named People, Family, Friends or Pets that do not hold faces. It writes the repairs on its own connection, outside the write lock. Neither library has anything to repair today. | fixed: *refactor: the tag tree's edits are services*. `tagpup.store.taxonomy.repair` writes only when there is something to repair, and then through the lock. The third repair went with #40. |
| 38 | 2026-09-24 | Five actions change a tag everywhere: renaming or deleting a node in TagPup's tree, merging or retiring a tag in TagTuner, and renaming a person in TagTuner. Each reaches a different set of places. Merging finds only photos carrying the tag itself, so a photo carrying only a tag under it keeps the old path, and the nodes under it stay in the tree. Renaming a person in TagTuner matches faces case-insensitively (`LOWER(name) = LOWER(?)`, which no index serves). It merges into the node if the new name already has one, where the tree's rename matches exactly and refuses. Only the tree's actions rewrite the library's taxonomy JSON. No two face names in either library differ only in case. | fixed: *fix: every way of changing a tag everywhere reaches the same places*. All five go through `tagpup.services.tags`. Merging and moving find the photos under the tag and move its branch in the tree, joining nodes already there. Renaming an unused tag keeps it under its new name. No tag goes under itself. Both person renames match any case without a function on the column. Every action keeps the JSON in step. Two differences are on purpose: the tree's rename refuses a path that is taken, and renaming a person joins it. Merging a tag leaves faces alone; renaming a person is where faces follow. |
| 39 | 2026-09-24 | Adding a tag and its missing ancestors to the tree is written five times: `TagTaxonomy.save_to_db`, twice in `seed_taxonomy_from_db`, `insert_tag_path_to_db`, and the create route. Each has its own copy of which new roots hold faces (#26). | fixed: *refactor: one writer of new tag-tree nodes*. `tagpup.store.taxonomy.add_path` writes every new node, with `vocabulary.FACE_ROOTS` as its rule, and a test keeps it the only writer. |
| 40 | 2026-09-24 | The Face Matching switch cannot be turned off for a root named People, Family, Friends or Pets. Turning it off clears the flag on the root and every node under it. The page then reloads the tree, and that read "repairs" every node under such a root back to holding faces (#37). The root reads off, and everyone under it is still a face to match. Nothing in either library is under such a root without the flag today. | fixed: *fix: turning face matching off is not undone by the next tree read*. The read no longer repairs the flag. Every node is made with its parent's flag, so a node without it is one somebody switched off. |
| 41 | 2026-09-24 | The tag tree counts a photo once for each of its tags under a node, not once per photo. A photo tagged `Family/Immediate/A` and `Family/Immediate/B` counts twice toward `Family/Immediate` and twice toward `Family`, so the counts beside parent nodes are too high. | fixed: *fix: the tag tree counts each photo once*. `tagpup.store.photos.tag_usage` collects a photo's levels before counting them. |
| 42 | 2026-09-24 | Naming faces in bulk (TagTuner's match-bulk, which the Identify Faces grids use) adds the person to the photo's people only when the assignment also displaced another name. The name is appended to the list, and the list is then compared with a copy of itself to decide whether to write. In `photo_index`, 35 manually named faces on 28 photos are missing from their photo's people, all named since the refresh of 2026-09-23 (#4); `kr-track` has none. | fixed in the code: *fix: naming faces in bulk lists the person on the photo*. The rows went on 2026-09-24 with every index entry whose file is gone *(owner, 2026-09-24)*: 64 photo rows, 1,259 faces (none of `photo_index`'s carried a decision; `kr-track`'s 2 were named) and 594 cached embeddings, each library backed up first. The doctor is clean on both. |
| 43 | 2026-09-24 | Six of TagTuner's face actions committed on a connection of their own, without the library's write lock: unmatch, unmatch-all, bulk unmatch, restore, both automatches, and removing a folder. The lock is what keeps two writers in one process from refusing each other with "database is locked". Automatch was one algorithm written twice, once for a photo and once for a folder. | fixed: *refactor: TagTuner's face actions are services* |
| 44 | 2026-09-24 | Deleting, merging or renaming a tag counts a photo whose index row lagged its file as one it could not rewrite. The photos to change come from the index, but the rewrite reads each file first and skips a photo whose file no longer carries the tag (#30), so fewer photos are rewritten than were found. A delete then answers that the tag was kept, and leaves the node in the tree though no photo carries it. A merge is not applied, and a rename warns that photos still carry the old tag. Found in review of phase 2; the service tests stood in for the whole rewrite and never met a skip. | fixed: *fix: a photo whose file already lost the tag is not one that could not be rewritten*, with tests that run the real rewrite. It needed #46 first. |
| 45 | 2026-09-24 | Folder automatch reads every named face of the folder with a prefix comparison that no index serves: a scan of all 226,830 faces, 1.4 s warm. Since phase 2 it runs inside the write lock, so every other face action in TagTuner waits behind it. Found in review of phase 2. | fixed: *fix: folder automatch reads only the photos it may change*. Inside the lock it looks up the names of each photo a name is proposed for, by the index on photo_path (`tagpup.store.faces.names_in_photo`). |
| 46 | 2026-09-24 | Reading a photo's keywords from its file (`tags_in_file`) says it raises when the file cannot be read, and does not when ExifTool cannot parse it: a file that is not an image (ExifTool calls it TXT) reads as having no tags. Replacing a tag then skips that photo as one that does not carry the tag, and rewrites its index row to no tags. A missing file does raise. Found while fixing #44, which would otherwise have let a tag rename wipe such rows. | fixed: *fix: a file ExifTool cannot read as a photo is not a photo without tags*. `tags_in_file` raises when ExifTool's MIME type for the file is not an image. |
| 47 | 2026-09-24 | TagTuner's Remove Folder chooses its folder with the system folder dialog, which lists the folders on disk, not the folders in the library. It cannot choose a folder that is gone, which is the usual reason to remove one: #42's 28 rows belong to a training folder the owner deleted, and there was no way to pick it. It also offers folders that were never indexed. | open, after the re-architecture (ARCHITECTURE.md, "After the re-architecture"): pick from the library's indexed folders, each with its photo count and whether it is still on disk. |
| 48 | 2026-09-24 | The schema is made in four places, each with its own idea of it: `PhotoIndex.load`, TagTuner's start-up (on a raw connection, without the write lock), the desktop runner (a faces table with no `excluded`, `excluded_reason` or `name_source`, the columns every screen now filters on), and the tag tree, which makes its own table when it saves. Migrations run as column checks on every load. A library made new never gets `idx_photos_document_id`, which only the migration that added the column created. A library reached only by its URL is never migrated at all: `kr-track` had no generation tables and none of the indexes that serve a path comparison without case, so its path lookups scanned and its caches never saw a change. Found in the inventory for phase 3. | fixed: *refactor: one owner for the schema, with versioned migrations*. `tagpup.store.schema.ensure` runs wherever a library is opened, each request that names one included. Tried on copies of both libraries: rows unchanged, generations carried over, 0.04 s. |
| 49 | 2026-09-24 | TagTuner's `/api/person-faces?name=Unmatched` counts every unnamed face and pages only the unnamed faces that are not excluded, so its total and `has_more` are wrong by the number excluded: in `kr-track` it reports 5,764 faces and has none to show. No page asks for it: the list stopped offering an "Unmatched" pseudo-person, and a comment says so. Found in the inventory for phase 3. | fixed: *fix: the person grid has no Unmatched pseudo-person to miscount*. The branch is gone; `Unmatched` is a name like any other, and nobody has it. |
| 50 | 2026-09-24 | TagTuner's people list with counts (`/api/people-with-counts`) looks up each person's tag-tree nodes with its own query, once per person: 284 queries in `photo_index`. The same answer is one query, which `tagpup.store.people.names` already makes. Found in the inventory for phase 3. | fixed: *fix: the people list reads the tag tree once, not once per person* (`tagpup.store.taxonomy.filed_people`). |
| 51 | 2026-09-24 | TagTuner remembers each photo's year by its path (`_path_year_cache`), for good and in every library. A time shift that moves a photo into another year leaves the old year in use for era-aware matching until the server restarts. Found in the inventory for phase 3. | open. |
| 52 | 2026-09-24 | The Suggest index decides whether to reload by a signature of the photos table (row count, highest rowid, sum of mtimes). A change to a row that leaves its mtime alone does not move it: `update_people`, which naming a face calls, changes who is in a photo without touching the file. Suggest then answers from the people the photos had when it loaded. Found in the inventory for phase 3. | fixed: *fix: Suggest's index sees who was named in a photo since it loaded*. It reloads when the `photos` generation moves, which every change to a row does. |
| 53 | 2026-09-24 | `tagpup.services.faces.exclude` accepts any reason. The page offers four, and `scripts/tidy_exclusion_reasons.py` exists because free text got in before; it can get in again through the API. Found in the inventory for phase 3. | fixed: *fix: a face is excluded only for a reason the app offers*. The service refuses any other; `tidy_exclusion_reasons.py` retires with nothing left to fold. |
| 54 | 2026-09-24 | `scripts/cache_all_crops.py` takes no arguments and ignores any it is given: `--help` runs it. It writes to `data/photo_index.db` beside the code, whatever library is in use, with no dry run, no backup and no write lock. Asking it for its usage while taking stock of the maintenance scripts wrote 4,053 crops into `photo_index` before it was stopped a minute in. The crops are cut exactly as the app cuts them (`tagpup.files.images.face_crop`), so they are the bytes the app would have cached on first showing each face; nothing was overwritten, and `quick_check` is ok. | fixed: *chore: retire five finished one-off scripts*. Crops are cut on demand, and phase 4 moves them to their own table. |
| 55 | 2026-09-24 | Migration 2 drops `faces_generation` and `taxonomy_generation`, but an older version of the app still running makes them again on its next load, and nothing takes them away after: `schema_version` already says 2. While it runs, that version reads its counters as 0 whenever they are missing, so its people cache and Identify grids stop seeing changes made by the new one. Found in review of phase 3. | fixed: *fix: opening a library takes away the counters an older version made again*. While an older version is still running it goes on reading 0; close the apps before installing. |
| 56 | 2026-09-24 | `schema.ensure` remembers a library it found current by file id and creation time. Copying a backup over the file in place keeps both on Windows, so a library restored from before the migration stays unmigrated, with no generations, until the process restarts. Found in review of phase 3. | fixed: *fix: a library restored over its own file is looked at again*. The memory is kept by modified time and size too. |
| 57 | 2026-09-24 | The `schema.ensure` a request makes for the library it names is unguarded: if the migration cannot run -- another process holding the write lock past the 30 s timeout, or an error -- every request to that library fails without an answer, page and static files included. Found in review of phase 3. | fixed: *fix: a request goes ahead when its library cannot be migrated just now*. It is logged, and the next request tries again. |
| 58 | 2026-09-24 | TagTuner's Identify grids are cached by the faces fingerprint, but they are built from photo rows too: a photo's people (the queue's groups), its date (the year of era matching), and a face's box, which the faces generation does not count. A tag saved in TagPup, a time shift or a rotated TIFF leaves the grids as they were until a face changes. Older than phase 3; the photos generation now makes it cheap to close, at the cost of rebuilding the grids -- up to a minute on `photo_index` -- after every photo write. Found in review of phase 3. | fixed for the queue *(owner, 2026-09-24)*: *fix: the Identify queue follows the people saved on its photos*. It is kept by the photos generation too. A person's grid stays keyed by the faces alone: it takes up to a minute to build. |
| 59 | 2026-09-24 | Migration 1 is marked as changing no data, but carries two rewrites of decisions from older libraries: 'Non Person' names onto `excluded`, and `is_people` onto `has_face`. A library that still has either is rewritten without the backup a data change gets. Neither of the owner's libraries has either. Found in review of phase 3. | fixed: *fix: migration 1 backs a library up when it has decisions to rewrite*; then gone with the rewrites themselves in *refactor: migration 1 makes today's tables and refuses older ones* *(owner, 2026-09-24)*. |
| 60 | 2026-09-24 | `tools/doctor.py` says a library's generations are counted when the `generations` table has its three rows; it does not look for the triggers that move them, nor for the old counters #55 describes. Found in review of phase 3. | fixed: *fix: the doctor checks what keeps the generations moving*. It looks for each row and trigger, and for the old counters. |
| 61 | 2026-09-24 | `tagpup_cli.py --test` writes its tag tree into the real `data/photo_index.db`. `TagTaxonomy` works out its database from the JSON file's name, and any name ending in `photo_taxonomy.json` -- the test mode's `test_photo_taxonomy.json` included -- maps to `photo_index.db` beside it; `index --test` then saves the tree there. Found in the inventory for phase 4. | fixed: *refactor: the tag tree lives in the library only*. The CLI works on one library (`get_db_path`), and `TagTaxonomy` takes it. |
| 62 | 2026-09-24 | A photo's CLIP embedding is stored twice, in `photos.embedding` and `embedding_cache`, keyed by the file's mtime and size. A keyword write changes the mtime, so the next Suggest misses the cache and embeds the photo again; a rotation leaves `photos.embedding` describing the picture before the turn; a change of model settings replaces the cache but not `photos.embedding`, so Suggest compares vectors from two models. Found in the inventory for phase 4. | open: phase 4, the embeddings table |
| 63 | 2026-09-24 | A photo's people are patched by seven face actions (`update_people`) with a rule of their own: unnaming a face takes the person off even when a keyword still names them, and a name is added by exact case where the real rule (`people_in_photo`) ignores case. Clustering, re-detection and tag-tree edits change who a photo's people are and update nothing. Found in the inventory for phase 4. | open: phase 4, the photo_people table |
| 64 | 2026-09-24 | Saved suggestions are dropped by nothing: deleting a photo, removing a folder or relinking a renamed one leaves them, and renaming a photo by saving a new title does not move them. Found in the inventory for phase 4. | open: phase 4, the suggestions table |
| 65 | 2026-09-24 | Removing a folder (`remove_under`) and pruning photos (`photos.remove`) leave the photos' cached embeddings behind. Found in the inventory for phase 4. | open: phase 4, the embeddings table |
| 66 | 2026-09-24 | Which roots hold faces is said by the tree (`has_face`) and by five lists in the code that disagree with it and each other: `vocabulary.FACE_ROOTS` (people, family, friends, pets: a new root of one of these names is made holding faces), `PeopleVocabulary.DEFAULT_ROOTS` and `taxonomy.PEOPLE_ROOTS` (family, friends, people, added to whatever the tree says), both servers (the tree's, plus pets), and the suggester and the CLI's `suggest` (family, friends, pets -- not people). So the CLI offers everyone under People to CLIP by name as a word, the suggester prompts for them as a thing rather than a person, and a library whose tree does not flag Family still has Family people. Both of the owner's libraries flag every face root they have. Raised by the owner. The sweep for duplicated rules found five more copies: the suggester's known people and the tags it passes on from neighbours (family, friends), the writer's suggested caption (family, friends), and the TagPup page's `isPersonTag` (family, friends, people, pets, ahead of the tree's flag) -- so people under People are passed on as tags, put among "others" in captions, and Pets or Friends are people to the page whatever the tree says. | fixed *(owner, 2026-09-24)*: *fix: which roots hold faces is the tree's to say*. The lists are gone; a new library is seeded with People as its one face root, an empty tree counts as a new library's, and the last face root cannot be deleted, retired, merged into a word or switched off. |
| 67 | 2026-09-24 | The TagPup page decides a photo's Date Taken with its own list of fields (`DATE_KEYS`), which reads `EXIF:ModifyDate` and `XMP:ModifyDate` where `tagpup.core.dates` deliberately does not. A row carrying only a modified date shows a date taken in the thumbnail, the panel, the folder's dates and the edit dialog, while the server sorts it as undated. The guard in `tests/test_dates.py` reads Python only. Found in the sweep for duplicated rules. | open |
| 68 | 2026-09-24 | TagTuner's buckets are named "Unknown Faces", "Ungrouped" and "Excluded" by the server and the page, but nothing stops a person being given one of those names: only "Unmatched", which no bucket uses any more, is refused, and only when renaming. A person named "Excluded" opens the excluded list; one named "Ungrouped" or "Unknown Faces" gets that bucket's filter. Found in the sweep for duplicated rules. | open |
| 69 | 2026-09-24 | How sure a match must be before a face is offered a name differs by screen: TagPup's face strip offers from 0.5, TagTuner from 0.70 (confident at 0.85), and TagTuner's page ranks at 0.75 and 0.60. A face at 0.55 reads "Name?" in one app and nothing in the other. Found in the sweep for duplicated rules. | fixed *(owner, 2026-09-24)*: *fix: one value to offer a face a name, one to name it unasked*. Every screen offers from 0.70 (`tagpup.core.clustering.OFFER_A_NAME`), measured. |
| 70 | 2026-09-24 | A tag suggestion is shown and applied in TagPup from a score of 0.6, but the CLI's `write`, the writer's default and the runner write from 0.5: the same suggestions write tags scoring 0.5-0.6 that the app never showed. Found in the sweep for duplicated rules. | open |
| 71 | 2026-09-24 | A person's era centroid -- the years around a photo their faces are averaged over -- is computed three times: twice in clustering by mean, once in TagTuner's person grid by geometric median, with the same windows. Clustering unnames a face below 0.80 of one centroid; the grid flags it below 0.85 of another. The suggester uses a fourth, with no era window. Found in the sweep for duplicated rules. | open |
| 72 | 2026-09-24 | What counts as a photo is written eight times (jpg, jpeg, png, tiff, tif, webp) and twice more with other members: the relink script adds .heic, and the image server's list adds bmp, gif, heic and heif. Relink can point a row, with its faces, at a file the index never scans; and `images.content_type` names only png and webp, so a TIFF, GIF or HEIC is sent as image/jpeg. Found in the sweep for duplicated rules. | open |
| 73 | 2026-09-24 | A library can be created under a name its URL cannot reach: `api`, `gui` and `gui_tagpup` are taken by the routes (`RESERVED_PATHS`), and `tagpup.core.library` allows them. Found in the sweep for duplicated rules. | open |
| 74 | 2026-09-24 | Rules written more than once that still agree, and will not stay so unless one place owns each: automatch's 0.8 (in three modules, the page's text and colours, and as a distance worked out by hand); clustering's 0.48; the page's match bands, written twice in one file; the tiny-background-face rule; the crop's size and quality; opening an image outside `tagpup.files` (the embedder); the words offered to CLIP (server and CLI) and the prompts; the embedding size guessed from the model name, three times and wrong for bigG; the embedder's own model defaults beside the config's; the `test_` prefix done by hand in the runner, the server and the CLI; the default library spelled as a working-directory path; the exclusion reasons and their default between service and page; the camera name for a time shift (server and page, twice); the Date Taken fields the page writes; the caption categories beside the seeded roots; the "Unknown" year; the ports and `find_available_port` in four launchers. Found in the sweep for duplicated rules. | open: a single-owner pass, each with a guard where the copies are in the pages |
| 75 | 2026-09-24 | About forty thresholds decide things about faces and tags, over three kinds of score (face-to-face or face-to-centroid cosine, CLIP similarity, and a tag's share of its neighbours), and they make seventeen kinds of decision -- a face is offered a name, named without asking, flagged as possibly wrong, unnamed by clustering; a tag is shown, written. The same decision has different values in different places (a face is offered a name from 0.5, 0.595, 0.60 or 0.70; confident at 0.75, 0.8, 0.82, 0.85 or 0.9), distances and cosines mix (a comment calls distance 0.90, cosine 0.595, "high-confidence"), and most values have no stated reason; the measured ones are TagTuner's suggestion floor and its Likely/Possible bands. Specs and the tutorial describe values the code no longer has (person match at 0.85, auto-apply at 0.75, clustering derived from 0.85 while using 0.885). Raised by the owner: each value should have a stated scenario and be revalidated. | open: the two face values are settled *(owner, 2026-09-24)* -- a name offered from 0.70, given unasked from 0.80, in `tagpup.core.clustering` with the measurement; the rest (flagging a name as possibly wrong, clustering's unnaming and radius, the page's bands, the tag values) are to be measured and decided |
| 76 | 2026-09-24 | A library several migrations behind is backed up once for every migration that rewrites data, each copy taken just before its own migration: opening an unmigrated photo_index with migrations 3 and 4 pending made two copies of 2.8 GB and more, and took two of the five backups a library keeps. The first copy already holds the library as it was for all of them. Found when `test_schema` saw two backups after migration 4 was added. | fixed: *fix: a library is backed up once, however many migrations it needs* |
| 77 | 2026-09-24 | Migration 4 as first written would not open a library where a face spelled its photo with the other separator (`D:/x/b.jpg` against a row `D:\x\b.jpg`): the rows it made for photos without one matched by `paths.key`, the faces by collation, which does not equate the separators, and the face found no photo. Neither real library has such a face. Found by the agent rewriting the tests for it. | fixed: *refactor: faces point at photos by id*; faces find their photo by `paths.key` |
| 78 | 2026-09-24 | `tools/doctor.py` reads a library without migrating it, and two of its checks join faces to photos by id: on a library not yet at migration 4 -- both real ones today, and every backup from before it -- it failed with `no such column` instead of saying the migrations were not applied. Found in review. | fixed: *refactor: faces point at photos by id*; the face checks wait for migration 4, which `schema_current` reports |
| 79 | 2026-09-24 | A TagPup started before migration 4 and still running after something newer opened the library reads faces by `photo_path`, which is gone. `face_names` swallows the error and returns nothing, so its next tag save rewrites the photo's people from keywords alone, and everyone known only by their face drops off, with a warning in the log. A copy started afterwards fails loudly on load. Migration 3 was one-way too, but old code failed loudly on it. Found in review. | left as is: every app is closed *(owner, 2026-09-24)*, so none started before the migration can outlive it |
| 80 | 2026-09-24 | Migration 4 rebuilt `faces` with AUTOINCREMENT from the faces that exist, so its counter fell from the highest id ever given to the highest still there: in kr-track, from 7,539 to 7,306, and 233 ids of deleted faces would be given out again, where a grid, an undo or the resolution trace from before could still name them. Found in review. | fixed: *refactor: faces point at photos by id*; the counter is carried over |
| 81 | 2026-09-24 | `photos.id` had no AUTOINCREMENT, so a new photo could take the id of the last one deleted, and a face whose photo row was deleted by something other than the store's deletes would join the new photo, with its name. Found in review. | fixed: *refactor: faces point at photos by id* |
| 82 | 2026-09-24 | The rows migration 4 makes for photos a face was on but that had no row kept the face's spelling rather than the stored form (`photos.ensure_row` uses `paths.stored`), so a lookup by the stored form missed them and the next face recorded made a second row. And where two rows shared a file, faces that named the second exactly went to the first. Neither real library has either. Found in review. | fixed: *refactor: faces point at photos by id*; a face goes to the row it names exactly first |
| 83 | 2026-09-24 | Migration 4 drops `photos` and `faces` to rebuild them, which on a connection with foreign keys on deletes every face and every crop first. It is safe because `schema._ensure` connects without them, and nothing said so. Found in review. | fixed: *refactor: faces point at photos by id*; the migration refuses a connection with foreign keys on |
