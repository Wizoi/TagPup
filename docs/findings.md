# Findings

This file records every review finding, bug report and known problem: what was found, and where it stands. A finding that exists only in a conversation gets lost, and a decision that is not written down gets made again.

- **Add a row when something is found**, before fixing it.
- **Status** is one of:
  - `open`
  - `fixed`: give the commit, by its hash or, for a row added in the same commit, its subject.
  - `not reproduced`: say what was tried.
  - `left as is`: say why.
  - `tabled`: say until when.
  - `planned`: give the phase in [ARCHITECTURE.md](ARCHITECTURE.md).
- **Decisions the owner makes** are marked *(owner, date)*. Don't reopen them without a new fact.
- **Never put real names here.** The library is photographs of real people.

Findings fixed before 2026-09-23 are in the git log (`git log --grep="^fix"`). This file starts with what was still open or decided on that day.

| # | Found | Finding | Status |
|---|---|---|---|
| 1 | 2026-09-23 | Faces are detected on the photo as stored, so a sideways photo (about 1 in 600, more as photos are rotated) is searched sideways. A rotated TIFF also keeps the embedding of its old turn. Detecting upright changes the coordinate system of stored face boxes, the crops, and the rotate logic. | tabled for a future discussion *(owner, 2026-09-23)* |
| 2 | 2026-09-23 | One of the library's two PNGs cannot be rotated. The rotate refuses and changes nothing. | left as is *(owner, 2026-09-23)* |
| 3 | 2026-09-23 | Six faces in `photo_index` were both excluded and named. | fixed: the owner kept the names *(2026-09-23)*; exclusions lifted, with a backup taken first (`photo_index.before-keep-names-20260923_180819.db`) |
| 4 | 2026-09-23 | Rows had people missing that their keywords or faces name: 741 names on 597 rows in one library, 885 names in the other. | fixed: `refresh_rows_from_files.py --apply` on both libraries (550 and 591 rows), with a backup taken first. Phase 4 derives people instead of storing them. |
| 5 | 2026-09-23 | Opening New Person takes about 1.7 s. Caching its embeddings would hold about 388 MB in memory. | open: the embeddings table holds photos' CLIP vectors; faces keep theirs in `faces`, so New Person reads what it read before. Measure again when it is next worked on. |
| 6 | 2026-09-23 | Duplicate face-resolution trace entries for paths that differ only in case. | not reproduced. Each library now writes its own trace file. |
| 7 | 2026-09-23 | `backups/` holds 28 GB in 41 copies. Scripts also wrote copies next to the code they ran from. | fixed *(owner, 2026-09-23)*: the old copies were deleted, keeping the newest of each library and the `.log`/`.txt` records. New backups go beside the library, in `data/backups/`, since *refactor: Library names a library and what belongs to it*. |
| 8 | 2026-09-23 | Eight TagTuner routes are reached only by tests: its copies of rename, time shift, delete, open in Explorer, rotate, save metadata and bulk tags, plus `/api/faces/recluster`, which no page calls. | fixed: *refactor: TagTuner loses the routes its page never called*. Its tests of them moved to TagPup's copies, which they had not been run against; the moves found #21. |
| 9 | 2026-09-23 | `runner.py` opened the database with `sqlite3.connect`, without WAL mode or the write lock. The database guard never saw it: each guard kept its own list of files, and that one skipped `runner.py` and the launchers. | fixed: *refactor: the foundation modules move into tagpup/*. Every guard now checks one list. |
| 10 | 2026-09-23 | `runner.py` keeps its own copy of the `photos` and `faces` schema, and two column migrations, separate from `PhotoIndex`'s. Two definitions of one schema drift apart. | fixed: 2d42646 (*refactor: one owner for the schema, with versioned migrations*, #48). The runner calls `tagpup.store.schema.ensure`. |
| 11 | 2026-09-23 | `tests/test_multiple_databases.py` changes the checkout's own `config.ini`: selecting a library through the API makes the server write `default_db` there. The test restores the old value afterwards, so a run stopped in between leaves the app pointing at a test library. | fixed: *refactor: one owner for config.ini, and TAGPUP_HOME*. The test runs in a home of its own and checks that the checkout's config is byte-for-byte unchanged. |
| 12 | 2026-09-23 | Moving the foundation modules into `tagpup/` (9d7e1c4) broke both measurement tools. Their sandbox copied a fixed list of folders that did not include `tagpup/`, so the sandbox server could not import its database module. Nothing tested a sandbox. | fixed: *fix: measurement sandboxes copy tagpup/ too*, with a test that imports both servers inside a sandbox that cannot see the repo |
| 13 | 2026-09-23 | The CLI and the servers name the main library's taxonomy file differently: `photo_taxonomy.json` (CLI) and `photo_index_taxonomy.json` (servers). The owner's `data/` holds both, written on different days and already different in size. The special case is in `tagpup_cli.get_db_paths` and in two places in `scripts/taxonomy.py` (`TagTaxonomy`, `seed_taxonomy_from_db`); `Library.taxonomy_file` follows the servers. | fixed: *refactor: the tag tree lives in the library only*. The CLI works on one library (`get_db_path`), and `TagTaxonomy` takes it. |
| 14 | 2026-09-23 | The test suite writes its libraries into the checkout's `data/`: about 20 databases in the owner's folder, some without the `test_` prefix (`created_db_2.db`, `unused.db`) and one with a 2.9 MB WAL left behind. A hard-coded exclusion list, copied into `runner.py` and both servers (one list since *refactor: one set of rules for the library picker*: `tagpup.core.library.NOT_LIBRARIES`), hides some of them from the library picker. | fixed: *test: the server tests' libraries are in homes of their own* (f99980b), *test: ExifTool is found without reading the checkout's settings* (3bd4952), *test: the rest of the suite leaves data/ and config.ini alone* (f691b33), *test: every test file runs across the cores, each in a home of its own* (702fa51), *refactor: the library picker hides nothing by name* (070e715), and *test: verify_workflow copies the library into a home of its own*. A test takes a home from `tests/own_home.py`; `tools/run_tests.py` gives each file a `TAGPUP_HOME` of its own as well, its lane for the checkout's files is empty, and `tests/test_tests_have_homes_of_their_own.py` fails a test file that names the checkout's `data/` or `config.ini`. The full suite, run either way, leaves `data/` empty. `NOT_LIBRARIES` is gone: every name in it was a test's file or the tag-embedding cache, which nothing has made since it moved into the library. The lane had missed `test_server_startup`, which started the app on the checkout's `data/photo_index.db`, and `test_verify_workflow_is_sandboxed`, whose script copies a library into the data folder -- found when the runner's empty home had none; the test_*.db files a run left in the owner's `data/` before this are still there to delete. |
| 15 | 2026-09-23 | Creating a library leaves it open: both servers' create handlers load a `PhotoIndex` for the new file and never close it, so the connection lasts until a garbage-collection pass. On Windows the file cannot be moved or deleted until then. Found when a test could not delete its temporary home. | fixed: *fix: creating a library closes it again*. One `create_library` replaces five copies (both servers, both launchers, the runner), and a test with garbage collection off deletes the new file. |
| 16 | 2026-09-23 | Selecting a library saves its name without checking that it exists. The next start then creates an empty library under that name. | open |
| 17 | 2026-09-23 | `config.ini` is tracked in git, but the app writes the last-opened library into it. Every switch shows up as a change, and `default_db` has been committed back and forth three times (fd8a9dc, be7e06c, 349c5fe). | fixed *(owner, 2026-09-23)*: *chore: config.ini is each installation's, not git's*. `config.example.ini` is tracked, setup copies it, and the defaults match it. |
| 18 | 2026-09-23 | Moving `db.py` into `tagpup/store/` (9d7e1c4) moved its default backup folder too: it was found two folders above the module, which became `tagpup/` -- inside the code, where the measurement sandbox would have copied every backup. No backup had landed there yet; no test covered the default folder. | fixed: *refactor: Library names a library and what belongs to it*. Backups go beside the library, and inside `tagpup/` only `tagpup/config.py` may find folders from `__file__`. |
| 19 | 2026-09-23 | `scripts/merge_duplicate_person_tags.py` turns tags into leaves by hand (`leaf_of`), which CLAUDE.md forbids; the vocabulary helpers own it. | fixed: *refactor: one reading of a tag*, with 54 other hand-written splits |
| 20 | 2026-09-23 | The indexer's per-photo write locks lived in `data/locks` under the working directory. The indexer a server starts runs in the code's folder, so one started by hand from anywhere else locked in a different folder, and the two could write the same photo at once. An installed copy would have made that the normal case. | fixed: *fix: indexers lock photos beside the library, not the working directory* |
| 21 | 2026-09-23 | A request with no library in its URL, or naming the startup library, went to `data/<its name>.db`, not to the file the server was started on. Same file while the library lives in `data/`, as the launchers arrange; a server started on one anywhere else served an empty namesake it created in `data/`. Found when moved tests put their library in a temp folder. | fixed: *fix: a server serves the library it was started on, wherever it is* |
| 22 | 2026-09-23 | TagPup's indexing messages tell the owner to "run Recluster" (`index_folder_with_cli`), but no page has a Recluster button: clustering is the runner's **Run Identity Resolution Clustering** or `tagpup_cli.py cluster-faces`. | fixed: *fix: adding a folder names the clustering button that exists*. A test ties the message to the runner's button label. Neither page shows the message today: TagTuner's reports only failures, and TagPup's sees no index (#34). |
| 23 | 2026-09-23 | One photo in `photo_index` carried a test tag holding a "\|", which some programs write between levels. Python read it as a separator in a few places and as part of a name in the rest. | fixed *(owner, 2026-09-23)*: corrected, then the whole test hierarchy removed at the owner's request, from the file, the index and the taxonomy. Backups first: `photo_index.before-fix-pipe-tag-20260923_214253.db`, `photo_index.before-remove-test-tags-20260923_214444.db` |
| 24 | 2026-09-23 | Nothing stops a tag or a person's name from holding a "\|", a "\\", a control character or an empty level. Typed into either page, it is written into the photo files as it is. | fixed *(owner, 2026-09-23)*: *fix: a tag other programs would misread is refused where it is set*. Both servers and both pages, held to one list of cases (`tests/tag_rules.json`). |
| 25 | 2026-09-23 | Adding a tag under one two or more levels deep builds junk nodes. "Jane" added under `Crew/Divers` made `Crew/Divers/Jane`, and also `Crew/Divers/Crew`, `Crew/Divers/Crew/Divers` and `Crew/Divers/Crew/Divers/Jane`; the reply's id named the last. Neither library holds any. `test_taxonomy_lifecycle.py` builds exactly this tree and checked only that the right node was there. | fixed: *fix: a tag added two levels down is one node, not four*. The test now checks the whole tree. |
| 26 | 2026-09-23 | Which roots hold people is written out by hand in 19 places, as five different sets, beside `TagTaxonomy.people_roots()`, which reads the library's own. The sets without People matter: kr-track files all 59 of its people under People, so the CLI's `suggest` offers each of them to CLIP by name (the server stopped doing that in `clip_candidates`), and the suggester's and the caption writer's checks do not count them as people. | open: phase 2, as the rules move into `tagpup.core` |
| 27 | 2026-09-23 | `TagTaxonomy.find_person_path` returns the first path it meets for a name, and it meets them in the order of a set, which can change from one run to the next. The suggester names recognised faces with it. `find_by_leaf`, `people_paths_for` and `repair_bare_person_tags.py` treat a name filed twice as ambiguous and leave it alone. Two people in `photo_index` are filed twice. | open |
| 28 | 2026-09-23 | A path typed into TagPup is written as typed. "People / Rowan Thackeray" creates the node `People/Rowan Thackeray`, but the photo gets the spaced form: `resolveTagOrPerson` returns the text rather than the tag it created, and nothing on the write path normalizes a tag. Neither library holds one. | fixed: *fix: a new tag is written as the tag tree spells it*. The page returns the tree's spelling, and both servers write a new tag in it. |
| 29 | 2026-09-23 | Editing a caption renames a photo that Smart Rename named (one carrying PreservedFileName) by splitting its name on " - ": the first part is taken as the grouping and the second as the index. The owner's groupings often hold " - " themselves (`<date> - <event>`), so "`<date> - <event> - 07 - <caption>`" became "`<date> - <event> - <new caption>`", its index gone. 10,795 photos in `photo_index` have that shape, and 3 of a random 30 of them carry PreservedFileName. The name was built by a second copy of Smart Rename's code, which also misread every name made in a format other than the default. | fixed *(owner, 2026-09-23)*: *fix: a Smart Rename grouping cannot hold " - "*. Renaming moves forward, so the fix is at the grouping: a name already made that way is put right when Smart Rename next runs on its photos, and caption renaming is left as it is. It still misreads names made in a format other than the default, which the owner does not use. |
| 30 | 2026-09-24 | Renaming or deleting a tag in the tag tree, merging tags, and renaming a person rewrite each photo's whole keyword set starting from the index's copy of its tags, not the file's. A keyword the file holds and the index does not -- written by another program, or since the last index -- is dropped from the file; one the index holds and the file does not is written into it. The bulk writers were moved to the file for this reason (`tags_in_file`); this one was not. A random 200 photos in each library agree with their files today, so nothing has been lost yet. | fixed: *fix: renaming a tag everywhere starts from what each file holds* |
| 31 | 2026-09-24 | TagTuner's Tags view loads every photo of a tag at full size. Its cards ask for `/api/photo-file?path=...&thumb=1`, and no server reads `thumb`, so each card downloads the original file, where every other grid asks for a copy with `size=`. | open |
| 32 | 2026-09-24 | 13 routes, 6 in TagPup and 7 in TagTuner, decode a path or a name twice: `parse_qs` has already decoded the query, and they call `unquote` on what it returns. A name holding "%" and two hex digits reaches them as a different name: `Scan%2001.jpg` becomes `Scan 01.jpg`, and the photo is not found. Neither library holds such a path today (0 of 68,327 and 0 of 1,129). | open |
| 33 | 2026-09-24 | An error page that names a photo cannot always be sent. `send_error` puts its message in the HTTP status line, which `http.server` encodes as Latin-1, so photo-file's 404 for a path holding any character outside Latin-1 (Greek, Cyrillic, most Central European letters) raises instead, and the browser sees the connection drop. The servers build 36 error messages with an f-string. | open |
| 34 | 2026-09-24 | Only tests call TagPup's `/api/folder/index-start`: its page lost the Index button when adding folders moved to TagTuner. The page still asks its own server about indexing when it opens a folder, "so an index started elsewhere still shows its progress here". But an index started in TagTuner runs in TagTuner's process, so TagPup's server answers "Ready" throughout. | planned: phase 5, where one process serves both apps from one queue |
| 35 | 2026-09-24 | TagTuner carries copies of two TagPup methods that nothing calls. No route reaches `handle_get_tags`, and no code calls `rescan_folder_to_cache`. The second is the only writer of TagTuner's `folder_cache`, and nothing reads it. Three more class attributes, `suggest_threads`, `model_lock` and `shared_embedder`, are declared and never used. | fixed: *refactor: TagTuner loses the copies nothing called* |
| 36 | 2026-09-24 | Marking a node as holding faces, or hiding it, marks the nodes under it with `tag LIKE '<path>/%'`. LIKE ignores case and reads `_` and `%` as wildcards, so marking `Club_A` would mark everything under `ClubXA` too. No tag in either library holds `_` or `%` today. | fixed: *fix: marking a branch marks only the tags under it*. `tagpup.store.taxonomy.sql_branch` matches the path and its `/`. |
| 37 | 2026-09-24 | Opening the tag tree first repairs it. Every GET of `/api/taxonomy/tree` looks for three things: nodes missing their parent, names holding `/`, and nodes under a root named People, Family, Friends or Pets that do not hold faces. It writes the repairs on its own connection, outside the write lock. Neither library has anything to repair today. | fixed: *refactor: the tag tree's edits are services*. `tagpup.store.taxonomy.repair` writes only when there is something to repair, and then through the lock. The third repair went with #40. |
| 38 | 2026-09-24 | Five actions change a tag everywhere: renaming or deleting a node in TagPup's tree, merging or retiring a tag in TagTuner, and renaming a person in TagTuner. Each reaches a different set of places. Merging finds only photos carrying the tag itself, so a photo carrying only a tag under it keeps the old path, and the nodes under it stay in the tree. Renaming a person in TagTuner matches faces case-insensitively (`LOWER(name) = LOWER(?)`, which no index serves). It merges into the node if the new name already has one, where the tree's rename matches exactly and refuses. Only the tree's actions rewrite the library's taxonomy JSON. No two face names in either library differ only in case. | fixed: *fix: every way of changing a tag everywhere reaches the same places*. All five go through `tagpup.services.tags`. Merging and moving find the photos under the tag and move its branch in the tree, joining nodes already there. Renaming an unused tag keeps it under its new name. No tag goes under itself. Both person renames match any case without a function on the column. Every action keeps the JSON in step. Two differences are on purpose: the tree's rename refuses a path that is taken, and renaming a person joins it. Merging a tag leaves faces alone; renaming a person is where faces follow. |
| 39 | 2026-09-24 | Adding a tag and its missing ancestors to the tree is written five times: `TagTaxonomy.save_to_db`, twice in `seed_taxonomy_from_db`, `insert_tag_path_to_db`, and the create route. Each has its own copy of which new roots hold faces (#26). | fixed: *refactor: one writer of new tag-tree nodes*. `tagpup.store.taxonomy.add_path` writes every new node, with `vocabulary.FACE_ROOTS` as its rule, and a test keeps it the only writer. |
| 40 | 2026-09-24 | The Face Matching switch cannot be turned off for a root named People, Family, Friends or Pets. Turning it off clears the flag on the root and every node under it. The page then reloads the tree, and that read "repairs" every node under such a root back to holding faces (#37). The root reads off, and everyone under it is still a face to match. Nothing in either library is under such a root without the flag today. | fixed: *fix: turning face matching off is not undone by the next tree read*. The read no longer repairs the flag. Every node is made with its parent's flag, so a node without it is one somebody switched off. |
| 41 | 2026-09-24 | The tag tree counts a photo once for each of its tags under a node, not once per photo. A photo tagged `Family/Immediate/A` and `Family/Immediate/B` counts twice toward `Family/Immediate` and twice toward `Family`, so the counts beside parent nodes are too high. | fixed: *fix: the tag tree counts each photo once*. `tagpup.store.photos.tag_usage` collects a photo's levels before counting them. |
| 42 | 2026-09-24 | Naming faces in bulk (TagTuner's match-bulk, which the Identify Faces grids use) adds the person to the photo's people only when the assignment also displaced another name. The name is appended to the list, and the list is then compared with a copy of itself to decide whether to write. In `photo_index`, 35 manually named faces on 28 photos are missing from their photo's people, all named since the refresh of 2026-09-23 (#4); `kr-track` has none. | fixed in the code: *fix: naming faces in bulk lists the person on the photo*. The rows went on 2026-09-24 with every index entry whose file is gone *(owner, 2026-09-24)*: 64 photo rows, 1,259 faces (none of `photo_index`'s carried a decision; `kr-track`'s 2 were named) and 594 cached embeddings, each library backed up first. The doctor is clean on both. |
| 43 | 2026-09-24 | Six of TagTuner's face actions committed on a connection of their own, without the library's write lock: unmatch, unmatch-all, bulk unmatch, restore, both automatches, and removing a folder. The lock is what keeps two writers in one process from refusing each other with "database is locked". Automatch was one algorithm written twice, once for a photo and once for a folder. | fixed: *refactor: TagTuner's face actions are services* |
| 44 | 2026-09-24 | Deleting, merging or renaming a tag counts a photo whose index row lagged its file as one it could not rewrite. The photos to change come from the index, but the rewrite reads each file first and skips a photo whose file no longer carries the tag (#30), so fewer photos are rewritten than were found. A delete then answers that the tag was kept, and leaves the node in the tree though no photo carries it. A merge is not applied, and a rename warns that photos still carry the old tag. Found in review of phase 2; the service tests stood in for the whole rewrite and never met a skip. | fixed: *fix: a photo whose file already lost the tag is not one that could not be rewritten*, with tests that run the real rewrite. It needed #46 first. |
| 45 | 2026-09-24 | Folder automatch reads every named face of the folder with a prefix comparison that no index serves: a scan of all 226,830 faces, 1.4 s warm. Since phase 2 it runs inside the write lock, so every other face action in TagTuner waits behind it. Found in review of phase 2. | fixed: *fix: folder automatch reads only the photos it may change*. Inside the lock it looks up the names of each photo a name is proposed for, by the index on photo_path (`tagpup.store.faces.names_in_photo`). |
| 46 | 2026-09-24 | Reading a photo's keywords from its file (`tags_in_file`) says it raises when the file cannot be read, and does not when ExifTool cannot parse it: a file that is not an image (ExifTool calls it TXT) reads as having no tags. Replacing a tag then skips that photo as one that does not carry the tag, and rewrites its index row to no tags. A missing file does raise. Found while fixing #44, which would otherwise have let a tag rename wipe such rows. | fixed: *fix: a file ExifTool cannot read as a photo is not a photo without tags*. `tags_in_file` raises when ExifTool's MIME type for the file is not an image. |
| 47 | 2026-09-24 | TagTuner's Remove Folder chooses its folder with the system folder dialog, which lists the folders on disk, not the folders in the library. It cannot choose a folder that is gone, which is the usual reason to remove one: #42's 28 rows belong to a training folder the owner deleted, and there was no way to pick it. It also offers folders that were never indexed. | open, after the re-architecture (ARCHITECTURE.md, "After the re-architecture"): pick from the library's indexed folders, each with its photo count and whether it is still on disk. |
| 48 | 2026-09-24 | The schema is made in four places, each with its own idea of it: `PhotoIndex.load`, TagTuner's start-up (on a raw connection, without the write lock), the desktop runner (a faces table with no `excluded`, `excluded_reason` or `name_source`, the columns every screen now filters on), and the tag tree, which makes its own table when it saves. Migrations run as column checks on every load. A library made new never gets `idx_photos_document_id`, which only the migration that added the column created. A library reached only by its URL is never migrated at all: `kr-track` had no generation tables and none of the indexes that serve a path comparison without case, so its path lookups scanned and its caches never saw a change. Found in the inventory for phase 3. | fixed: *refactor: one owner for the schema, with versioned migrations*. `tagpup.store.schema.ensure` runs wherever a library is opened, each request that names one included. Tried on copies of both libraries: rows unchanged, generations carried over, 0.04 s. |
| 49 | 2026-09-24 | TagTuner's `/api/person-faces?name=Unmatched` counts every unnamed face and pages only the unnamed faces that are not excluded, so its total and `has_more` are wrong by the number excluded: in `kr-track` it reports 5,764 faces and has none to show. No page asks for it: the list stopped offering an "Unmatched" pseudo-person, and a comment says so. Found in the inventory for phase 3. | fixed: *fix: the person grid has no Unmatched pseudo-person to miscount*. The branch is gone; `Unmatched` is a name like any other, and nobody has it. |
| 50 | 2026-09-24 | TagTuner's people list with counts (`/api/people-with-counts`) looks up each person's tag-tree nodes with its own query, once per person: 284 queries in `photo_index`. The same answer is one query, which `tagpup.store.people.names` already makes. Found in the inventory for phase 3. | fixed: *fix: the people list reads the tag tree once, not once per person* (`tagpup.store.taxonomy.filed_people`). |
| 51 | 2026-09-24 | TagTuner remembers each photo's year by its path (`_path_year_cache`), for good and in every library. A time shift that moves a photo into another year leaves the old year in use for era-aware matching until the server restarts. Found in the inventory for phase 3. | open. |
| 52 | 2026-09-24 | The Suggest index decides whether to reload by a signature of the photos table (row count, highest rowid, sum of mtimes). A change to a row that leaves its mtime alone does not move it: `update_people`, which naming a face calls, changes who is in a photo without touching the file. Suggest then answers from the people the photos had when it loaded. Found in the inventory for phase 3. | fixed: *fix: Suggest's index sees who was named in a photo since it loaded*. It reloads when the `photos` generation moves, which every change to a row does. |
| 53 | 2026-09-24 | `tagpup.services.faces.exclude` accepts any reason. The page offers four, and `scripts/tidy_exclusion_reasons.py` exists because free text got in before; it can get in again through the API. Found in the inventory for phase 3. | fixed: *fix: a face is excluded only for a reason the app offers*. The service refuses any other; `tidy_exclusion_reasons.py` retires with nothing left to fold. |
| 54 | 2026-09-24 | `scripts/cache_all_crops.py` takes no arguments and ignores any it is given: `--help` runs it. It writes to `data/photo_index.db` beside the code, whatever library is in use, with no dry run, no backup and no write lock. Asking it for its usage while taking stock of the maintenance scripts wrote 4,053 crops into `photo_index` before it was stopped a minute in. The crops are cut exactly as the app cuts them (`tagpup.files.images.face_crop`), so they are the bytes the app would have cached on first showing each face; nothing was overwritten, and `quick_check` is ok. | fixed: *chore: retire five finished one-off scripts*. Crops are cut on demand, and phase 4 moves them to their own table. |
| 55 | 2026-09-24 | Migration 2 drops `faces_generation` and `taxonomy_generation`, but an older version of the app still running makes them again on its next load, and nothing takes them away after: `schema_version` already says 2. While it runs, that version reads its counters as 0 whenever they are missing, so its people cache and Identify grids stop seeing changes made by the new one. Found in review of phase 3. | fixed: *fix: opening a library takes away the counters an older version made again*. While an older version is still running it goes on reading 0; close the apps before installing. |
| 56 | 2026-09-24 | `schema.ensure` remembers a library it found current by file id and creation time. Copying a backup over the file in place keeps both on Windows, so a library restored from before the migration stays unmigrated, with no generations, until the process restarts. Found in review of phase 3. | fixed: *fix: a library restored over its own file is looked at again*. The memory is kept by modified time and size too. |
| 57 | 2026-09-24 | The `schema.ensure` a request makes for the library it names is unguarded: if the migration cannot run -- another process holding the write lock past the 30 s timeout, or an error -- every request to that library fails without an answer, page and static files included. Found in review of phase 3. | fixed: *fix: a request goes ahead when its library cannot be migrated just now*. It is logged, and the next request tries again. |
| 58 | 2026-09-24 | TagTuner's Identify grids are cached by the faces fingerprint, but they are built from photo rows too: a photo's people (the queue's groups), its date (the year of era matching), and a face's box, which the faces generation does not count. A tag saved in TagPup, a time shift or a rotated TIFF leaves the grids as they were until a face changes. Older than phase 3; the photos generation now makes it cheap to close, at the cost of rebuilding the grids -- up to a minute on `photo_index` -- after every photo write. Found in review of phase 3. | fixed for the queue *(owner, 2026-09-24)*: *fix: the Identify queue follows the people saved on its photos*. It is kept by the photos generation too. A person's grid stays keyed by the faces alone: it takes up to a minute to build. |
| 59 | 2026-09-24 | Migration 1 is marked as changing no data, but carries two rewrites of decisions from older libraries: 'Non Person' names onto `excluded`, and `is_people` onto `has_face`. A library that still has either is rewritten without the backup a data change gets. Neither of the owner's libraries has either. Found in review of phase 3. | fixed: *fix: migration 1 backs a library up when it has decisions to rewrite*; then gone with the rewrites themselves in *refactor: migration 1 makes today's tables and refuses older ones* *(owner, 2026-09-24)*. |
| 60 | 2026-09-24 | `tools/doctor.py` says a library's generations are counted when the `generations` table has its three rows; it does not look for the triggers that move them, nor for the old counters #55 describes. Found in review of phase 3. | fixed: *fix: the doctor checks what keeps the generations moving*. It looks for each row and trigger, and for the old counters. |
| 61 | 2026-09-24 | `tagpup_cli.py --test` writes its tag tree into the real `data/photo_index.db`. `TagTaxonomy` works out its database from the JSON file's name, and any name ending in `photo_taxonomy.json` -- the test mode's `test_photo_taxonomy.json` included -- maps to `photo_index.db` beside it; `index --test` then saves the tree there. Found in the inventory for phase 4. | fixed: *refactor: the tag tree lives in the library only*. The CLI works on one library (`get_db_path`), and `TagTaxonomy` takes it. |
| 62 | 2026-09-24 | A photo's CLIP embedding is stored twice, in `photos.embedding` and `embedding_cache`, keyed by the file's mtime and size. A keyword write changes the mtime, so the next Suggest misses the cache and embeds the photo again; a rotation leaves `photos.embedding` describing the picture before the turn; a change of model settings replaces the cache but not `photos.embedding`, so Suggest compares vectors from two models. Found in the inventory for phase 4. | fixed: *refactor: one embeddings table, by photo and model*. The app's metadata writes carry a vector's stamp forward and a rotation takes it away. Migration 5 keeps each vector under the model its cache row named: 78 search vectors (1 in kr-track, 77 in photo_index) had none, and the next index of their folders computes them again. |
| 63 | 2026-09-24 | A photo's people are patched by seven face actions (`update_people`) with a rule of their own: unnaming a face takes the person off even when a keyword still names them, and a name is added by exact case where the real rule (`people_in_photo`) ignores case. Clustering, re-detection and tag-tree edits change who a photo's people are and update nothing. Found in the inventory for phase 4. | fixed: *refactor: each photo's people in photo_people, by one rule*. `people.rebuild` alone writes a photo's people; the faces store rebuilds at every write, and tree edits rebuild the photos whose keywords changed meaning. Rebuilt by the rule, both libraries of 2026-09-24 listed who they had, bar one photo in another order; the doctor's `people_out_of_date` watches for a writer that forgets. |
| 64 | 2026-09-24 | Saved suggestions are dropped by nothing: deleting a photo, removing a folder or relinking a renamed one leaves them, and renaming a photo by saving a new title does not move them. Found in the inventory for phase 4. | fixed: *refactor: suggestions kept by photo, in the library*. A row goes with its photo by a trigger, and a rename moves nothing. Migration 7 took in the saved entries of photos the library has: 77 in kr-track, 3 in photo_index, whose other 307 were for files that are gone. |
| 65 | 2026-09-24 | Removing a folder (`remove_under`) and pruning photos (`photos.remove`) leave the photos' cached embeddings behind. Found in the inventory for phase 4. | fixed: *refactor: one embeddings table, by photo and model*. A trigger takes a photo's vectors with its row; migration 5 dropped the 24,838 cached vectors of photos no longer in photo_index. |
| 66 | 2026-09-24 | Which roots hold faces is said by the tree (`has_face`) and by five lists in the code that disagree with it and each other: `vocabulary.FACE_ROOTS` (people, family, friends, pets: a new root of one of these names is made holding faces), `PeopleVocabulary.DEFAULT_ROOTS` and `taxonomy.PEOPLE_ROOTS` (family, friends, people, added to whatever the tree says), both servers (the tree's, plus pets), and the suggester and the CLI's `suggest` (family, friends, pets -- not people). So the CLI offers everyone under People to CLIP by name as a word, the suggester prompts for them as a thing rather than a person, and a library whose tree does not flag Family still has Family people. Both of the owner's libraries flag every face root they have. Raised by the owner. The sweep for duplicated rules found five more copies: the suggester's known people and the tags it passes on from neighbours (family, friends), the writer's suggested caption (family, friends), and the TagPup page's `isPersonTag` (family, friends, people, pets, ahead of the tree's flag) -- so people under People are passed on as tags, put among "others" in captions, and Pets or Friends are people to the page whatever the tree says. | fixed *(owner, 2026-09-24)*: *fix: which roots hold faces is the tree's to say*. The lists are gone; a new library is seeded with People as its one face root, an empty tree counts as a new library's, and the last face root cannot be deleted, retired, merged into a word or switched off. |
| 67 | 2026-09-24 | The TagPup page decides a photo's Date Taken with its own list of fields (`DATE_KEYS`), which reads `EXIF:ModifyDate` and `XMP:ModifyDate` where `tagpup.core.dates` deliberately does not. A row carrying only a modified date shows a date taken in the thumbnail, the panel, the folder's dates and the edit dialog, while the server sorts it as undated. The guard in `tests/test_dates.py` reads Python only. Found in the sweep for duplicated rules. | fixed: the server side in *fix: a face is compared with the person's closest face, in one place* (photos.taken and photos.year, migration 8); the pages in *fix: the TagPup page reads when a photo was taken from the server*: every record it is sent carries `taken` (tagpup.core.dates), and a guard fails a date field named in either page. |
| 68 | 2026-09-24 | TagTuner's buckets are named "Unknown Faces", "Ungrouped" and "Excluded" by the server and the page, but nothing stops a person being given one of those names: only "Unmatched", which no bucket uses any more, is refused, and only when renaming. A person named "Excluded" opens the excluded list; one named "Ungrouped" or "Unknown Faces" gets that bucket's filter. Found in the sweep for duplicated rules. | fixed: *fix: nobody can be called one of TagTuner's lists*. `tagpup.core.vocabulary.BUCKETS` names them for the server, the page keeps one copy a test holds to it, and `problem_with_name` refuses them and Unmatched whatever the case. Nobody in either library had such a name. |
| 69 | 2026-09-24 | How sure a match must be before a face is offered a name differs by screen: TagPup's face strip offers from 0.5, TagTuner from 0.70 (confident at 0.85), and TagTuner's page ranks at 0.75 and 0.60. A face at 0.55 reads "Name?" in one app and nothing in the other. Found in the sweep for duplicated rules. | fixed *(owner, 2026-09-24)*: *fix: one value to offer a face a name, one to name it unasked*. Every screen offers from 0.70 (`tagpup.core.clustering.OFFER_A_NAME`), measured. |
| 70 | 2026-09-24 | A tag suggestion is shown and applied in TagPup from a score of 0.6, but the CLI's `write`, the writer's default and the runner write from 0.5: the same suggestions write tags scoring 0.5-0.6 that the app never showed. Found in the sweep for duplicated rules. | fixed: *fix: one value for showing and writing a suggested tag*. `tagpup.core.suggesting.OFFER_A_TAG`, 0.60 *(owner, 2026-09-24)*, measured: 85% of tags from 0.6 are right, 45% of those between 0.5 and 0.6. |
| 71 | 2026-09-24 | A person's era centroid -- the years around a photo their faces are averaged over -- is computed three times: twice in clustering by mean, once in TagTuner's person grid by geometric median, with the same windows. Clustering unnames a face below 0.80 of one centroid; the grid flags it below 0.85 of another. The suggester uses a fourth, with no era window. Found in the sweep for duplicated rules. | fixed: *fix: a face is compared with the person's closest face, in one place*. `tagpup.core.clustering.KnownFaces` compares a face with the person's closest single face of the years around its photo, never one of its own photo; clustering, TagTuner's person grid and the suggester ask it. Measured on kr-track, the closest face tells the owner's people from strangers better than the mean or the median, which scored alike. |
| 72 | 2026-09-24 | What counts as a photo is written eight times (jpg, jpeg, png, tiff, tif, webp) and twice more with other members: the relink script adds .heic, and the image server's list adds bmp, gif, heic and heif. Relink can point a row, with its faces, at a file the index never scans; and `images.content_type` names only png and webp, so a TIFF, GIF or HEIC is sent as image/jpeg. Found in the sweep for duplicated rules. | fixed: *fix: what counts as a photo is said once*. `tagpup.files.images.PHOTO_TYPES`, with each type's Content-Type; `is_photo` and `photos_under`, which the five folder walks share. Relink no longer takes .heic, and the servers send only what the index lists. |
| 73 | 2026-09-24 | A library can be created under a name its URL cannot reach: `api`, `gui` and `gui_tagpup` are taken by the routes (`RESERVED_PATHS`), and `tagpup.core.library` allows them. Found in the sweep for duplicated rules. | fixed: *fix: a library cannot take the name of the app's own pages*. `tagpup.core.library.ROUTES`, refused by `problem_with_new_name` whatever the case, and what the server routes. |
| 74 | 2026-09-24 | Rules written more than once that still agree, and will not stay so unless one place owns each: automatch's 0.8 (in three modules, the page's text and colours, and as a distance worked out by hand); clustering's 0.48; the page's match bands, written twice in one file; the tiny-background-face rule; the crop's size and quality; opening an image outside `tagpup.files` (the embedder); the words offered to CLIP (server and CLI) and the prompts; the embedding size guessed from the model name, three times and wrong for bigG; the embedder's own model defaults beside the config's; the `test_` prefix done by hand in the runner, the server and the CLI; the default library spelled as a working-directory path; the exclusion reasons and their default between service and page; the camera name for a time shift (server and page, twice); the Date Taken fields the page writes; the caption categories beside the seeded roots; the "Unknown" year; the ports and `find_available_port` in four launchers. Found in the sweep for duplicated rules. | fixed: a single-owner pass, each with a guard where the copies are in the pages; the tiny background face: `clustering.background_faces`, used by clustering and the suggester (tests/test_rules_have_one_owner.py); the crop: `images.CROP_SIZE`/`CROP_QUALITY` and `images.crop_jpeg`, used by face detection and the tutorial seeding; opening a photo: `images.opened` (upright or as stored) for the embedder and face detection, and Pillow's size limit raised there alone, not in four modules; the words and prompts for CLIP: `suggesting.zero_shot_words` (server and CLI) and `suggesting.clip_prompt` (the suggester); the embedding size: `embedder.output_dim` asks open_clip (bigG is 1280, not 512) and `embedder.stored_mismatch` compares the library's vectors with it, for index, suggest and search; the embedder's defaults: none of its own; `ClipEmbedder` takes `config.embedder_settings()` for any setting not given and refuses one it does not know; the `test_` prefix: `library.for_mode` and `library.is_test_library`, for the runner, the TagPup server, localserver and the CLI; where a library is: `config.default_library` and `config.library_path` for PhotoIndex (no default now), both servers' handlers and start_server, and the bulk and measuring scripts' --db/--source; the exclusion reasons: `services.faces.DEFAULT_REASON`/`IGNORED_CLUSTER`/`EXCLUSION_REASONS`; the server asks them, and TagTuner's page keeps a copy held to them by a test; the camera of a time shift: `fields.camera_of`/`on_camera` with `CAMERA_FIELDS`, `UNKNOWN_CAMERA`, `ALL_CAMERAS`; the TagPup page's one copy (was two) held to them by a test; the caption categories and the seeded roots: `vocabulary.ACTIVITY_ROOT`, `PLACE_ROOTS`, `NEW_LIBRARY_ROOTS` and `CONTEXT_ROOTS`, for the seed, the caption and the suggester; the "Unknown" year: `dates.UNKNOWN_YEAR` and `dates.shown_year` for TagTuner's server and the scan records; TagTuner's page's one copy (was ten spellings) held to it by a test; the ports: `localserver.TAGTUNER_PORT`/`TAGPUP_PORT` and `localserver.find_available_port`, for the two launchers, the runner, both start_servers and the screenshot script |
| 75 | 2026-09-24 | About forty thresholds decide things about faces and tags, over three kinds of score (face-to-face or face-to-centroid cosine, CLIP similarity, and a tag's share of its neighbours), and they make seventeen kinds of decision -- a face is offered a name, named without asking, flagged as possibly wrong, unnamed by clustering; a tag is shown, written. The same decision has different values in different places (a face is offered a name from 0.5, 0.595, 0.60 or 0.70; confident at 0.75, 0.8, 0.82, 0.85 or 0.9), distances and cosines mix (a comment calls distance 0.90, cosine 0.595, "high-confidence"), and most values have no stated reason; the measured ones are TagTuner's suggestion floor and its Likely/Possible bands. Specs and the tutorial describe values the code no longer has (person match at 0.85, auto-apply at 0.75, clustering derived from 0.85 while using 0.885). Raised by the owner: each value should have a stated scenario and be revalidated. | fixed: every value decided *(owner, 2026-09-24)* and measured on the owner's data: a name offered from 0.70, given unasked from 0.80, flagged below 0.70 and kept by clustering from 0.80 against the closest face; faces grouped from 0.885; Likely from 0.80 and Possible from 0.70 in every band, named by the server (*fix: TagTuner's bands and clustering's radius come from tagpup.core.clustering*); a tag shown and written from 0.60. |
| 76 | 2026-09-24 | A library several migrations behind is backed up once for every migration that rewrites data, each copy taken just before its own migration: opening an unmigrated photo_index with migrations 3 and 4 pending made two copies of 2.8 GB and more, and took two of the five backups a library keeps. The first copy already holds the library as it was for all of them. Found when `test_schema` saw two backups after migration 4 was added. | fixed: *fix: a library is backed up once, however many migrations it needs* |
| 77 | 2026-09-24 | Migration 4 as first written would not open a library where a face spelled its photo with the other separator (`D:/x/b.jpg` against a row `D:\x\b.jpg`): the rows it made for photos without one matched by `paths.key`, the faces by collation, which does not equate the separators, and the face found no photo. Neither real library has such a face. Found by the agent rewriting the tests for it. | fixed: *refactor: faces point at photos by id*; faces find their photo by `paths.key` |
| 78 | 2026-09-24 | `tools/doctor.py` reads a library without migrating it, and two of its checks join faces to photos by id: on a library not yet at migration 4 -- both real ones today, and every backup from before it -- it failed with `no such column` instead of saying the migrations were not applied. Found in review. | fixed: *refactor: faces point at photos by id*; the face checks wait for migration 4, which `schema_current` reports |
| 79 | 2026-09-24 | A TagPup started before migration 4 and still running after something newer opened the library reads faces by `photo_path`, which is gone. `face_names` swallows the error and returns nothing, so its next tag save rewrites the photo's people from keywords alone, and everyone known only by their face drops off, with a warning in the log. A copy started afterwards fails loudly on load. Migration 3 was one-way too, but old code failed loudly on it. Found in review. | left as is: every app is closed *(owner, 2026-09-24)*, so none started before the migration can outlive it |
| 80 | 2026-09-24 | Migration 4 rebuilt `faces` with AUTOINCREMENT from the faces that exist, so its counter fell from the highest id ever given to the highest still there: in kr-track, from 7,539 to 7,306, and 233 ids of deleted faces would be given out again, where a grid, an undo or the resolution trace from before could still name them. Found in review. | fixed: *refactor: faces point at photos by id*; the counter is carried over |
| 81 | 2026-09-24 | `photos.id` had no AUTOINCREMENT, so a new photo could take the id of the last one deleted, and a face whose photo row was deleted by something other than the store's deletes would join the new photo, with its name. Found in review. | fixed: *refactor: faces point at photos by id* |
| 82 | 2026-09-24 | The rows migration 4 makes for photos a face was on but that had no row kept the face's spelling rather than the stored form (`photos.ensure_row` uses `paths.stored`), so a lookup by the stored form missed them and the next face recorded made a second row. And where two rows shared a file, faces that named the second exactly went to the first. Neither real library has either. Found in review. | fixed: *refactor: faces point at photos by id*; a face goes to the row it names exactly first |
| 83 | 2026-09-24 | Migration 4 drops `photos` and `faces` to rebuild them, which on a connection with foreign keys on deletes every face and every crop first. It is safe because `schema._ensure` connects without them, and nothing said so. Found in review. | fixed: *refactor: faces point at photos by id*; the migration refuses a connection with foreign keys on |
| 84 | 2026-09-24 | A photo's vector was carried forward from the stamp on its row, not from the file just before the app wrote it. A file changed elsewhere -- turned in Explorer, edited in another program -- keeps its row's old stamp until it is read again, so the next tag written in TagPup moved the old vector onto the new file: Suggest used it as current and the CLI's index skipped the photo for good. Replacing a tag in a photo whose file already differed wrote nothing and still carried it forward. The test for carrying a vector forward changed the file outside the app, and agreed with the bug. Found in review of f3daf4f. | fixed: *fix: a vector is carried forward only over the app's own write*; each writer stats the file before it writes and passes that stamp |
| 85 | 2026-09-24 | `photos.record_indexed` accepted an embedding with no model and dropped it without a word: `prepare_test_environment.py` seeded two CLIP vectors that never reached the library. Found in review of f3daf4f. | fixed: *fix: a vector is carried forward only over the app's own write*; an embedding without a model is refused |
| 86 | 2026-09-24 | A Suggest embedding a photo while it was rotated stamped the vector from before the turn with the file's stamp after it, the stat being taken once the photo was embedded: the rotation took the vector away and the Suggest put it back as current. Found in review of f3daf4f. | fixed: *fix: a vector is carried forward only over the app's own write*; the stamp is the file's before it is opened |
| 87 | 2026-09-24 | Renaming a person rewrites only the photos carrying their tag as a path (`People/Old Name`); a photo naming them by the bare keyword `Old Name` keeps it. Before 5c5961e the rename edited each photo's stored list of people, which hid that; once people are rebuilt from the keywords and the tree, such a photo lists nobody after the rename -- out of search and of the Identify grouping, and not counted in the rename's photos affected. In photo_index, 11 photos name one person only so. Found in review of 5c5961e. | fixed: *fix: renaming a person rewrites their bare keyword too* |
| 88 | 2026-09-24 | `people.follow_tree` read the first segment of every keyword, and a keyword that is only a separator (`/`, `|`) has none: toggling a root's face flag, or repairing the tree, failed and rolled back. None in either library. Found in review of 5c5961e. | fixed: *fix: renaming a person rewrites their bare keyword too* |
| 89 | 2026-09-24 | Two writers of a source of people did not rebuild: restoring an excluded face (a face both named and excluded, which the doctor watches for, counts again once restored) and recording what a time shift read back (a file's person fields feed the rule). Neither changes anyone in either library today. Found in review of 5c5961e. | fixed: *fix: renaming a person rewrites their bare keyword too* |
| 90 | 2026-09-24 | With suggestions kept by photo, a renamed photo's row kept the suggester's output as made, naming the old path, and a folder's consensus wrote its result back by that path: after a rename it updated nothing, and said nothing. Found by the agent moving the tests to the table, before it was committed. | fixed: *refactor: suggestions kept by photo, in the library*; consensus is taken over each row's current path |
| 91 | 2026-09-24 | A folder's saved suggestions were read as its photos directly in it, but a run suggests for what the folder's scan found, which walks its subfolders: Apply All found no suggestions for a photo in a subfolder, and every run suggested for those photos again. Found by the agent moving the tests to the table, before it was committed. | fixed: *refactor: suggestions kept by photo, in the library*; a folder's suggestions are its photos at any depth, as its scan is |
| 92 | 2026-09-24 | Saved suggestions were matched to a run's photos by exact spelling: the row's on one side, the folder scan's walked path on the other. Suggest on a folder typed in another case -- or opened from the page's cache after a restart -- suggested for every photo again, took consensus over none, and the page, looking suggestions up by the walked path, showed none: all 77 of kr-track's saved folder, in a trial on a copy. Before, both sides came from the scan. Found in review of ef37fe2. | fixed: *fix: saved suggestions are found however the folder is spelled*; the run matches rows by paths.key and hands them back under the scan's spelling |
| 93 | 2026-09-24 | A status poll read the saved rows before it read the run's state, so a poll straddling the end of a run could say "completed" with the rows from before consensus: the page keeps what it has on "completed", and Apply All then wrote the consensus tags, which the panel never showed. About one poll in five on a folder of 8,700 photos. Found in review of ef37fe2. | fixed: *fix: saved suggestions are found however the folder is spelled*; the run's state is read first, so the rows are never older than it |
| 94 | 2026-09-24 | The folder scan compared a row's mtime with the file's, and a row made for a photo Suggest saw but the index never read has none: the scan failed with TypeError, so Refresh, a restart or the scan after Apply All failed on any folder Suggest had been run on. It predates ef37fe2, which made such rows for its suggestions too. Found in review of ef37fe2. | fixed: *fix: saved suggestions are found however the folder is spelled*; a row with no stamp is read again, as the store's docstring says it is |
| 95 | 2026-09-24 | One suggestion that could not be written -- the library locked past every retry, as by a compact -- ended the whole run in "error" and skipped its consensus. Found in review of ef37fe2. | fixed: *fix: saved suggestions are found however the folder is spelled*; the photo is left unsuggested for the next run, and the run goes on |
| 96 | 2026-09-24 | Migration 7 took a photo listed under two folders from the last, an error over a success, and counted entries rather than rows. None in either library's file. Found in review of ef37fe2. | fixed: *fix: saved suggestions are found however the folder is spelled*; a success is not replaced by an error |
| 97 | 2026-09-24 | A rename the index skips, its destination already having a row, left the renamed photo's suggestions on the old row, where the file's saved suggestions used to move regardless. The old row describes a file no longer there, and the sync of phase 8 is what reconciles it. Found in review of ef37fe2. | left as is: the skip is reported, and phase 8 reconciles rows with files |
| 98 | 2026-09-24 | Clustering cleared both faces of a photo that resolved to one person, re-matched the photo's unused people tags by resemblance, and only then put back a name given by hand: a tie between the hand-named face and another could give the other the same name, and the photo had it twice. It passed while the hand-named face was part of the average it was compared with, which it no longer is. Found moving clustering to the closest face (#71). | fixed: *fix: a face is compared with the person's closest face, in one place*; a decision made by hand holds through every step of a photo's resolution |
| 99 | 2026-09-24 | TagPup's server warms its embedder on a thread that opens the startup library (`PhotoIndex(db_path).load`, which makes the folder and the file when they are missing) and keeps it open until the process ends. A thread that starts after the file has gone makes an empty library in its place. Found in #14: a test's library, and the `data/` folder around it, were there again after its home had been deleted. `tests/own_home.py` deletes what is left once the process ends. | fixed: #113; the warm-up loads the models and opens no library |
| 100 | 2026-09-24 | The apps start on a library named in config.ini (`default_db`, preset to photo_index), and write the file whenever a library is chosen: a setting of one installation kept in a file the app edits, and a library opened that nobody asked for. | fixed: *feat: the browser remembers the library; a bare URL asks*, *feat: the CLI is told which library it works on*, *feat: the server neither reports nor remembers a chosen library* |
| 101 | 2026-09-24 | One full run of `tools/run_tests.py` in six failed one file, counting 1,427 tests where the others counted 1,469 -- a file that did not load, or ended early. It was the first run after the #14 agent's own runs, whose detached cleanup of test homes (tests/own_home.py) may still have been going; its output was not kept, so which file is not known. Five runs after it passed. | open: `tools/run_tests.py` should keep a failed run's output, so the next one names itself |
| 102 | 2026-09-24 | Two scripts write their output into whatever folder they are run from: `scripts/generate_screenshots.py` its two server logs (`gui_server.log`, `tuner_server.log`), and `tagpup_cli.py suggest` its `suggestions.json` by default. Run from the checkout, they left those files at its root beside three more logs from June; the app's own logs go to `data/logs` (tagpup.logs). Found clearing the checkout of files nothing uses. | open, in part: the screenshot tool starts the one server and writes no log of its own (*feat: everything that starts a server starts tagpup_web.py*); `tagpup_cli.py suggest` still writes `suggestions.json` into the working folder by default |
| 103 | 2026-09-24 | TagTuner's page reads `data.success` on every reply, refusals included (thirteen places), and its old `send_json_error` sent `{"success": false, "error": ...}`; the skeleton's `responses.error` sent `{"error": ...}` alone, so a refusal through it would read as `undefined`. TagPup's page treats the key missing as false. Found porting the routes. | fixed: *feat: TagPup's and TagTuner's routes are Flask blueprints*; `responses.error` carries `success: false` for both apps, and the tuner's `_refuse` delegates to it |
| 104 | 2026-09-24 | The old TagTuner refused every POST with 409 while a library was being clustered, the picker's `/api/databases/create` included. The picker is the skeleton's blueprint now, so the guard (`tuner_routes.refuse_writes_while_clustering`) covers TagTuner's own routes only; the picker touches no faces. | decided: the guard covers the tuner blueprint |
| 105 | 2026-09-24 | The old TagTuner read handlers opened a write-capable connection for every read, so a screen refresh took its turn behind the busy timeout like a write. The reads in `tagpup.services.identify`, `people.with_counts`, `tags.listing` and `tags.photos_carrying` open the library read-only; same rows, same query shapes. | done in the port |
| 106 | 2026-09-24 | The old handler read the fingerprint and the rows of a cached Identify answer on one connection; the cache lookup now reads the fingerprint on a connection of its own (`tagpup.jobs.identify`) and a miss reads fingerprint and rows together on another. A write landing between the two costs one more build later, never a stale answer: the value is stamped with the fingerprint read before its rows. | decided: one extra fingerprint query per cache hit, no change in what can be served |
| 107 | 2026-09-24 | docs/SPEC_TAGTUNER.md said the Identify queue's counts are photo counts; the route has counted faces (`"unit": "face"`, `photos` beside) since the sidebar's three units were made one. | fixed: 9755e42 (*docs: TagTuner's spec names the rules, not the numbers they replaced*) |
| 108 | 2026-09-24 | docs/SPEC_TAGTUNER.md said `/api/person-faces` marks outliers "as similarity < 0.85" and the pill bands at 0.85; since #75 the route uses `tagpup.core.clustering.looks_wrong` and `band`. | fixed: 9755e42; the spec names the rules |
| 109 | 2026-09-24 | `tests/test_stability.py` held two classes, one TagTuner's and one TagPup's (`TestPhotoActions`, which started the socket server and reached into the handler's folder cache); each port took its own. | fixed: both classes on the test client, in the routes commit |
| 110 | 2026-09-24 | Three guards failed in the worktree mid-port: `test_db_access.test_every_launcher_is_checked` (the launcher list named `tagtuner.py`), `test_rules_have_one_owner.TheAppsPorts` (8080/8090 in `tagpup_web.py`, `runner.py`, `tagpup/web/app.py`) and `WhatClipIsAsked` (`scripts/suggest_models.py` called `zero_shot_words` over two lines). | fixed: *chore: the two old servers, their launchers and localserver retire*; the ports' owner is `tagpup_web.PORTS`, the launcher list is the new one, the call is on one line |
| 111 | 2026-09-24 | `tagpup/web/desktop.py` is where the folder dialog, "show in Explorer" and "open" live; both blueprints import it rather than keeping a copy of `localserver.ask_for_folder`. | none: recorded so the shared owner is known |
| 112 | 2026-09-24 | The suggester, the CLIP embedder, the face models and `PhotoIndex` still live in `scripts/` (suggester.py, embedder.py, faces.py, index.py), and `tests/test_layers.py` forbids the package importing them, so the Flask routes cannot build a suggestion run's model. Moving them into `tagpup.ml` is phase 3's remaining line and needs their `config.ini` reads passed in. | fixed: phase 5.5 (*feat: tagpup.runtime*); the seam and `scripts/suggest_models.py` are gone |
| 113 | 2026-09-24 | TagPup's warm-up (#99) made a `PhotoIndex` on the startup library and loaded it on a thread that kept it open until the process ended. | fixed in `scripts/suggest_models.warm_up`: it loads CLIP and the face models only; `tests/test_suggest_reads_people_once.test_warm_up_opens_no_library` fails if a `PhotoIndex` is made |
| 114 | 2026-09-24 | TagPup's folder scan read new photos without the library's people vocabulary, so the people in each scanned record were derived from the default face roots alone; the index and the time-shift read-back used the library's. A library whose people are filed under a root other than People showed them as tags, not people, until the next index. | fixed in `tagpup.services.photos.scan_folder` / `read_folder`, which hand the reader `taxonomy.people_vocabulary` as the indexer does. Not proven against the old code with a test: it needs ExifTool and a library with a face root not named People; a follow-up |
| 115 | 2026-09-24 | `/api/photo-faces` for a photo with no faces answered `{"faces": [], "total": 0}` without `unmatched`, while the spec says every reply carries all three. | fixed in `tagpup.services.faces.panel`; `test_tagpup_server_api.TestPhotoFaces` checks the three keys |
| 116 | 2026-09-24 | Thirteen old TagPup routes decoded a query path twice (#32). The port keeps the second decoding, in one helper (`tagpup.web.tagpup_routes._wanted_path`, and `responses.photo` for photo-file). | left as is, on purpose: byte-for-byte with the old routes; closing #32 for TagPup is one line there |
| 117 | 2026-09-24 | TagPup's old error replies were `{"success": false, "error": msg}`; the skeleton's `responses.error` answered `{"error": msg}`. | folded into #103: one shape for both apps |
| 118 | 2026-09-24 | Folder autocompletion (`/api/autocomplete-folder`) walks this machine's drives; it is not a library action. | decided: it stays in `tagpup.web.tagpup_routes`, as the folder dialog does by the 2026-09-24 decision, rather than becoming a service |
| 119 | 2026-09-24 | `tagpup_server.indexed_tags_for_photo` had no caller but its test: bulk writes start from the file since #30. | dropped in the port; `test_bulk_writes_update_the_index` checks the same spelling rule through `tagpup.store.photos.read_tags` |
| 120 | 2026-09-24 | `tests/test_people_resolved_on_write`'s guard only checked the `write_keyword_fields` calls in `tagpup_server.py` passed `db_path`; the three service writers and the CLI writer resolve people themselves and were unguarded. | fixed: the guard walks every `write_keywords` call in `tagpup/services/*.py` and `scripts/writer.py` and requires the tags to come from `vocabulary.resolve_people` |
| 121 | 2026-09-24 | `test_stability.TestPhotoActions.test_api_photo_delete` sent a real temp file to this machine's Recycle Bin on every run. | fixed in the port: a plain remove, as the other delete tests do; the assertions are unchanged |
| 122 | 2026-09-24 | `tests/test_db_access.test_every_launcher_is_checked` failed on HEAD mid-phase: `tagtuner.py` was in `code_snapshot.LAUNCHERS` but not among the checked sources. | fixed in the retirement commit: the list is `runner.py`, `tagpup_cli.py`, `tagpup_web.py` |
| 123 | 2026-09-24 | Suggest had no model in any app not started by `tagpup_web.py`: the port moved the model out of the run (#112), and the measurement sandbox (`scripts/measure_suggest_folder.py`), `measure_identify_faces.py` and `verify_workflow.py` each wrote a launcher of their own that served the apps bare, so a run over a folder with photos ended in "Suggest has no model" and the harness waited out its timeout. Found in review of the port; confirmed by running. | fixed: *feat: one owner for starting, running and stopping processes*; each starts its copy's `tagpup_web.py`, the one launcher, which installs the models |
| 124 | 2026-09-24 | A test run put a Windows Terminal window on the desktop for every test process, 140 a run, and one for every server a test started. The test-home reaper (`tests/own_home.py`) was started with `DETACHED_PROCESS`; the venv's `python.exe` is a launcher that starts the real interpreter as a child, and a child with no console to inherit is given a new, visible one. Every spawn called `subprocess` itself and decided for itself whether the child got a window; most never decided. It took watching the desktop's process list to find which spawn it was, after a fix per site had quieted the others. | fixed: b18e8de (the reaper) and *feat: one owner for starting, running and stopping processes*: `tagpup.core.processes` owns `start`, `run`, `kill_tree` and `is_alive`, every child gets a hidden console, and `tests/test_processes_single_owner.py` fails any other module that calls `subprocess` or spells a flag |
| 125 | 2026-09-24 | A folder opened while empty stayed empty until Refresh: the port cached the empty scan like any other, where the old server sent `[]` and kept nothing. Photos copied in after the first look never showed, and a time shift on the folder found "no photos". Found in review of the port; confirmed by running. | fixed: *feat: TagPup's and TagTuner's routes are Flask blueprints*; an empty folder is walked again on each open, `tests/test_an_empty_folder_is_scanned_again.py` |
| 126 | 2026-09-24 | The tests that proved a background worker stays with the URL-named library (#44) now run the worker on the test thread: `test_database_context_isolation` patches `SuggestionRuns.start` with a synchronous run, and `test_multiple_databases` runs the index queue by hand. What replaced the thread check is a signature guard in `test_database_axis_guard` and an AST guard for `threading.local`. The index runner is still exercised on its thread by `test_tuner_server_api.TestQueueRunner`; the Suggest worker is not. The code reads correctly (closures carry the Library); a weakened guard, not a defect. | open: one end-to-end Suggest test on the real thread, through a route, would close it |
| 127 | 2026-09-24 | TagPup's JSON replies were no longer sent `no-store`: the tuner blueprint had its own after_request for it and the tagpup blueprint none, while both old servers set it on every JSON reply. | fixed: *feat: TagPup's and TagTuner's routes are Flask blueprints*; the factory sets it for every JSON reply of either app |
| 128 | 2026-09-24 | Once the CLI took its models from `tagpup.runtime`, `tests/test_functional_cli.py` loaded the real ViT-H-14 weights (a request to the Hugging Face hub, then a GPU load) and the face models mid-port. It, `test_cli_path_spelling` and `test_reindex_keeps_face_decisions` patched `embedder.ClipEmbedder` / `faces.FaceProcessor` by name; when the CLI stopped using those names the patches still applied and silently stopped mattering. Nothing in the suite said "a test loaded a model". Found porting phase 5.5. | fixed: *refactor: the models move into tagpup.ml*: the three patch `tagpup.ml.clip.ClipModel` / `tagpup.ml.faces.FaceModel`, and both loaders refuse in a test run (`tagpup.ml.refuse_in_tests`, `tests/test_no_test_loads_a_model.py`) |
| 129 | 2026-09-24 | The first guard against loading weights in a test held only under `tools/run_tests.py`: `tests/__init__.py` set the flag, and `unittest discover -s tests` (the CLAUDE.md command) never imports the tests package. A single file run that way loaded ViT-B-32 and the face models. Found in review of phase 5.5; confirmed by running. | fixed: *refactor: the models move into tagpup.ml*: the loaders also know a run by its main program (unittest, or a file in tests/), and the tests that start a server pass the flag on; the reviewer's reproduction passes with the flag unset |
| 130 | 2026-09-24 | Importing `tagpup.services.search` paid about 3 s for `open_clip` (and `identities` about 1 s for scikit-learn): the full suite went from 42 s to 71 s mid-port. | fixed in the port: the model modules are imported where a model is first built. Suite 39-41 s |
| 131 | 2026-09-24 | Suggest's candidate words from the settings were read once, when the runtime was made at the server's start; the old code read them as each run began, so an edit reached the next run without a restart. Found in review of phase 5.5. | fixed: *feat: tagpup.runtime*: a server hands the runtime the settings reader (`tagpup.config.load`), and the words are read at each run; `test_the_candidate_words_are_read_at_each_run`, failing on the port first |
| 132 | 2026-09-24 | `stats`, `list` and `remove` in the CLI, which touch no model, parsed the face settings once the runtime read every setting as it was made: a malformed `min_face_size` failed them. Found in review of phase 5.5. | fixed: *feat: tagpup.runtime*: the face settings are read when the face models are first built; `test_a_command_with_no_face_model_reads_no_face_settings`, failing on the port first |
| 133 | 2026-09-24 | `test_warm_up_opens_no_library` could not fail: its patch raised, and the warm-up catches and logs whatever a model raises. The port had also dropped its only assertion. Found in review of phase 5.5; confirmed by a mutation that opened a library. | fixed: the test counts what was opened and asserts no error was logged |
| 134 | 2026-09-24 | Decision: `tagpup.services.search.PhotoIndex(db_path, model=None)` loads the photos without vectors when given no model key, and refuses to write one. It took the settings' model when given none, which the package may not read. Every caller that ships passes one; the settings default lives on in the `scripts/index.py` shim for the tests that use it. | decided |
| 135 | 2026-09-24 | Decision: `tagpup.jobs.suggestions` is handed its models with each run's work, `work_for(library, photos, models)`, not by `SuggestionRuns`' constructor: `runs_for` is reached by routes that never start a run, so a constructor argument would be whichever route asked first. A route takes them from `app.config["RUNTIME"]`. | decided |
| 136 | 2026-09-24 | Decision: a `TagSuggester` given no face model does no on-the-fly detection; it used to build one into a module-level slot for itself. The runtime and the CLI's `suggest` give it one. The `scripts/suggester.py` shim keeps the slot for the two behaviour anchors that set it. | fixed: phase 6.5 (*refactor: no shims; writer and metadata go to their owners*) |
| 137 | 2026-09-24 | Decision: the caption made from a photo's tags moved to `tagpup.core.suggesting.caption_from_tags`, and the year read from a photo's metadata to `tagpup.core.dates.record_year`: rules, which the package's suggester and resolver need and may not import from `scripts/`. | decided |
| 138 | 2026-09-24 | The error "Detected faces for ... were NOT saved" now logs under `tagpup.services.faces` (it was `tagpup_cli.index`). The CLI's `cluster-faces` no longer loads the tag tree, which it passed to the resolver and the resolver never read. | done in the port |
| 139 | 2026-09-24 | Shims left in `scripts/` after phase 5.5, imported only by tests: `embedder.py` (`test_clip_sees_photos_upright`), `faces.py` (`test_face_clustering_rules`, `test_face_exclusion`, `test_stability`), `index.py` (seven test files: face wrappers, the settings-default model), `suggester.py` (the two anchors: the face-model slot). None builds a model. | fixed: phase 6.5 (*refactor: no shims; writer and metadata go to their owners*) |
| 140 | 2026-09-25 | Decision: the harness evaluates a page's modules as one *strict* script (`"use strict"` first). A browser runs modules strict, and each `app.js` had only ever run sloppy; all 528 page tests passed under strict unchanged. In strict eval, top-level `let`/`const`/`function` also stay out of `window`, as a module's do. | decided |
| 141 | 2026-09-25 | Decision: `web/common/api.js` has `api.fetch(path, options)` beside `api.url`, `api.json` and `api.image`: 37 of the pages' 74 requests read the Response (`res.ok`, `res.status`, an abort) rather than its JSON. `api.json` is exactly the `.then(res => res.json())` it replaced, whatever the status. With no library in the URL, `api.fetch` rejects every `/api/` request but the picker's, as the fetch monkeypatch did. | decided |
| 142 | 2026-09-25 | The library is now read from the page's URL at each request, not once when the page loaded; the same answer, since the pages only change the query string. `api.js` treats `common` as no library's name. | done in the port |
| 143 | 2026-09-25 | Decision: `common` is reserved as a library name (`tagpup.core.library.ROUTES`): a page opened at `/` imports `/common/api.js`, which would otherwise be taken for a library called `common`. `gui` and `gui_tagpup` stay reserved, so no library takes a name an old link still asks for. | decided |
| 144 | 2026-09-25 | Decision: `photoAlreadyHas(photo, tag, isPersonTag)` takes the page's `isPersonTag`: which roots hold people is the tag tree's, which only the page has loaded, and a shared module holds no page state. | decided |
| 145 | 2026-09-25 | Decision: `initDatabaseSelector(select, createButton, { beforeLeaving })`. The two copies were the same but for what choosing another library does first: TagPup asks about unsaved edits and puts the list back if you stay, TagTuner goes at once. | decided |
| 146 | 2026-09-25 | TagPup's `baseName(p)` and TagTuner's `basename(p)` both give a path's last segment but differ at the edges: for null TagPup's gives '' and TagTuner's "null"; for "/" TagPup's gives '' and TagTuner's "/". | open: one is right; it belongs in `paths.js` once that is decided |
| 147 | 2026-09-25 | `unsaved.js`, `dialogs.js` and `status.js` were not made: TagTuner has no unsaved-edit handling and no status line, and the pages' dialogs share no code. ARCHITECTURE.md's Frontend section listed them. | decided: the Frontend section names only the modules that exist |
| 148 | 2026-09-25 | What the harness's one scope cannot see, now guarded: a copy of a shared helper inside a page's closure shadows the import silently (`common-modules.test.mjs`), and a module using another's name without importing it works in the tests but is a ReferenceError in a browser (the loader refuses it). | done |
| 149 | 2026-09-25 | Decision: `web/package.json` says `"type": "module"`, so node imports `web/common/*.js` in tests without warning. It is copied with `web/` and never served. | decided |
| 150 | 2026-09-25 | database-routing's "an already-prefixed request is left alone" and "non-API requests are not rewritten" called `window.fetch` directly: with the monkeypatch gone they would have passed on anything. They now ask `api.url`/`api.fetch`, and a new test fails on a `fetch(` or an `/api/` image `src` in either page that bypasses `api.js`. | done |
| 151 | 2026-09-25 | Decision: TagPup's page is split by feature into 17 modules in `web/tagpup/`. main.js keeps the wiring, in the original order, and the start-up. State is one object (`state.js`). Calls back up the import order (14 of them: the list, grid and photo redraws, selectPhoto, the suggestion panel and others) go through `upper` in `hooks.js`, which main.js fills in at its top level, because the page tests refuse an import cycle. `tests/frontend/tagpup-modules.test.mjs` holds `upper` to the calls made through it and fails a module-level `let` or a function in main.js. | decided |
| 152 | 2026-09-25 | The page tests (`harness.mjs`) took a local or parameter of a shared module for a use of a page's top-level name: `photoAlreadyHas(photo, tag, isPersonTag)` in `web/common/vocabulary.js` failed the whole TagPup page once `isPersonTag` became a module's export. TagPup's function was renamed `namesAPerson` to keep the tests running. | fixed: *test: the page loader gives each module a scope of its own*; the rename stays, harmless |
| 153 | 2026-09-25 | Three guards read code out of `web/tagpup/main.js` by path, and the split moved it: `tag-rules.test.mjs` (groupingProblem, normalizeTag) and `test_rules_have_one_owner.TheCameraOfATimeShift` (CAMERA_FIELDS) failed, and `test_dates.test_the_pages_read_no_date_field_of_their_own` still passed while checking a file that no longer held the code. | fixed: *refactor: each page is split by feature*; the guards read every module of the page |
| 154 | 2026-09-25 | Decision: TagPup's elements are looked up when `elements.js` runs, not at DOMContentLoaded. A page's module scripts run after parsing, so the same elements are found. `isResizing` stays local to the sidebar resizer's wiring rather than going into `state`. | decided |
| 155 | 2026-09-25 | Decision: the page loader refuses an import cycle, so TagTuner's modules are layered, each importing only from those below it. Four calls go back up, each a write refreshing what is on screen (`selectPhoto`, `fetchPeopleWithCounts`, `selectPerson`, `refreshSidebarQuietly`, from assign.js and new-person.js), through `upper` in `web/tuner/hooks.js`, filled in by main.js before anything runs. | decided |
| 156 | 2026-09-25 | Decision: each TagTuner feature adds its listeners in one exported `wire<Feature>()`, which main.js calls after the start-up fetches, in the order the page first added each feature's listeners. Across features that changes the order listeners are added; every (target, event) pair is distinct, and the only wiring that does more than add a listener (the saved people sort) still runs after start-up. | decided |
| 157 | 2026-09-25 | Decision: TagTuner's element lookups and initial state run when its modules are evaluated, not on DOMContentLoaded. Every id they look up is written in `web/tuner/index.html`, and a browser runs module scripts after the page is parsed. | decided |
| 158 | 2026-09-25 | Six guards read `web/tuner/main.js` by path (test_bucket_names_have_one_owner, test_face_values_have_one_owner, test_rules_have_one_owner twice, test_dates, dom-contract three times); after the split some failed and the rest passed while checking nothing. They read every module of the page now (`shipped_sources.page_source`, `pageSource`), and a violation placed in any module fails each. | fixed: *refactor: each page is split by feature* |
| 159 | 2026-09-25 | The page loader demanded an import for any name another module declared, even where the name was only a local shadowing it: grid-parts.js and people.js import elements.js's `tabMatches`, `tabOutliers`, `tabLowConf` and never use them; TagPup renamed `isPersonTag` to `namesAPerson` to get past it. | fixed: *test: the page loader gives each module a scope of its own*; the unused imports and the rename stay, harmless |
| 160 | 2026-09-25 | `web/tuner/elements.js` keeps the page's comment on why `tagViewContent` is declared early ("a `const` further down would be reached before its line had run"), a hazard modules no longer have; it was moved word for word, and `tagViewContent` stayed there with it though only sidebar.js uses it. | open |
| 161 | 2026-09-25 | `selectPhoto` and `updatePeopleDatalist` are declared in both pages, as before the split. They are different functions of the same name (checked: the text differs); the guard compares text, not names. | decided |
| 162 | 2026-09-25 | The checks that a page keeps its state in one object, lists in `hooks.js` exactly the calls made through `upper`, fills each in `main.js` and starts without an uncaught error covered TagPup's page only; TagTuner met them by hand. Found in review of phase 6. | fixed: *test: both pages' modules are held to one state object and a filled upper* |
| 163 | 2026-09-25 | The page loader's unimported-name check exempted a name the module binds anywhere (a local in one function hides a bare use in another), and checked only exported names, so a call to another module's non-exported function passed; both are a ReferenceError in a browser on that path. Nothing in the pages does either today (checked by a cross-reference). Found in review of phase 6. | fixed in part: *test: both pages' modules are held...* also fails a non-exported name; open: exempt a name only where it is bound, not anywhere in the module |
| 164 | 2026-09-25 | `tests/frontend/tag-vocabulary.test.mjs` ("the app starts itself last") guards against start-up reaching a closure `let` before its line has run, which the modules' shape now rules out: main.js declares no state. It still holds the start-up block to the end, but its comment describes a bug that can no longer happen there. Found in review of phase 6. | open: housekeeping |
| 165 | 2026-09-25 | Three spellings of what a page module's name may be: `tagpup/web/app.py` (`MODULE_NAME`), `tagpup/web/libraries.py` (`PAGE_MODULE`) and the harness's `fileFor`. They agree today; no test holds them together, so a module named outside one would load in the tests and 404 in the browser, or the reverse. Found in review of phase 6. | open |
| 166 | 2026-09-25 | The two behaviour anchors could not tell a wired face model from none: with `faces` discarded both still passed, since the scoring stub returns no faces and the decisions file asserted only that detection was skipped. It predates phase 6.5 (the old module slot set to None passed too), the #128 pattern carried over. Found in review of phase 6.5; confirmed by running. | fixed: 180cdd0; a photo with no face rows must be detected once by the model handed in, and fails with the model discarded |
| 167 | 2026-09-25 | The reviewer's instructions opened the real library with `sys.path.insert(0, 'scripts'); import db`, which phase 6.5 deleted: the review step that matters most, checking against real rows, would have failed on its first command. | fixed: *docs: phases 6 and 6.5 are done*; it imports `tagpup.store.db` |
