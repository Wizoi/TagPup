torch --index-url https://download.pytorch.org/whl/cu121
torchvision --index-url https://download.pytorch.org/whl/cu121
open-clip-torch
faiss-cpu
pyexiftool
rich
click
tqdm
Pillow
facenet-pytorch
scikit-learn
# The web layer (tagpup.web): the apps' routes, and the server that runs both.
flask
waitress
# The MCP server Claude works with a library through (tagpup.mcp), over stdio.
mcp
# Drives the real app in a real browser: screenshots, and the end-to-end
# click-latency measurements in scripts/measure_identify_faces.py.
playwright
# Samples a running server's threads without restarting it (`py-spy dump --pid N`):
# the reloader makes attaching the only way to look inside one that is misbehaving.
py-spy
